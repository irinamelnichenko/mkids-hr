# Джерело правди

**`~/Desktop/BOT/HR mkids_bot/_backend/crm_script.js`** — єдине джерело правди для бекенду.

| | |
|---|---|
| Версія у репозиторії | **v7.343** |
| md5 | `d2e97245bedf0f362b655f9a4d24940c` |
| Розмір | 2084930 байт |
| Зафіксовано | 2026-09-25 |
| Версія у проді | **v7.343** — задеплоєно 25.09.2026; бот рахунків: підпис caption/reply/наступне повідомлення, розбивка на статті; readsGroupText=true |
| Перевірка | `action=ping` → `pong v7.343` ✅ |

## Як це працює

Бекенд **не деплоїться з git**. Файл вставляється вручну в редактор Apps Script.
Тому git — це реєстр того, що *має* бути в проді, а не те, що там є автоматично.

Після будь-якої правки в редакторі: скопіювати код назад сюди, звірити md5, закомітити.
Розбіжність md5 = локальна копія і прод розійшлись.

Перевірка версії проду:
```
curl -sL "https://script.google.com/macros/s/AKfycbyTSUVlaN4-PpXe47zCSmhVs0Qxy1FDXG_XsB4zcKNpqBxdhDtS9ibM4YFGkGjmPQDFWQ/exec?action=ping"
```

## v7.342: редагування бюджетів OPEX у системі (лише CFO)

- Бекенд: `getOpexBudgetGrid(loc)` — значення й формули колонки «Бюджет» кожного місяця (статті 3–30 + рядки
  «Кількість дітей / груп / основного персоналу» за назвою), напряму з файлу; `opexSetBudget` — dryRun за
  замовч., лише CFO (роль із токена, інакше actorId), лок, перевірка назви рядка та expected (файл змінили руками
  → CONFLICT, нічого не пишеться), формула → число (як при ручному записі), журнал «OPEX_Бюджет_Правки»
  (коли · хто · локація · стаття · місяць · клітинка · було · стало · формула), скидання кешу локації;
  `getOpexBudgetLog`. Перевірено на макеті: dryRun, не-CFO, конфлікт, зсунутий рядок, «Підсумок», запис+журнал.
- opex.html (режим локації, лише CFO): «✏️ Редагувати бюджет» — таблиця 12 міс × статті/кількості, ƒ для формул,
  підсвітка змін, «Зберегти» → dryRun → підтвердження списку → окреме підтвердження для формул → запис →
  перечитування файлу повз кеш; «🕘 Історія правок». Директорка — лише перегляд. sw v7.342.

**Перевірка в проді (25.09).** Пробні правки Осокорок без запису: AD25 «Методична частина» жовт. 4 200 → 4 500,
AG4 «Комунальні» лист. 109 696,8 → 60 000 (ƒ `=AD4*2`), AD36 «Кількість дітей» жовт. 63 → 66 (ƒ `=AA36+AE41`);
конфлікт (очікувала 400 000 при 402 332,5) — нічого не записано; директорка — PERM_DENIED; файл після прогонів
незмінний. Справжній запис AD25 4 200 → 4 500 і назад 4 200: файл, getOpexData (кеш скинуто) і журнал — ок.

## v7.341: OPEX — паралельне читання, кеш по локаціях, збережене одразу

Заміри перед змінами: getOpexOverview 17–22 с (17 файлів по черзі, без серверного кешу), getOpexData 5,5–7,2 с,
getOpexExpensesLog 5,1–5,7 с (підтягувався лише на першому кліку статті), getLocations ~4 с — у CFO ПЕРЕД оглядом;
ping 3,7–4,1 с. Кеш у браузері — 5 хв, далі спінер.
- бекенд: _opexRegistry + _opexReadGrids ('fast' = fetchAll → Sheets API, запасний SpreadsheetApp) + _opexParseGrid
  (спільний для getOpexData й огляду: огляд = сума тих самих статей) + кеш по локації 15 хв (ver_opex_<лок>, скидає
  opexAddExpenses); nocache=1 — напряму. Старі _getOpexDataOld/_getOpexOverviewOld — для diagOpexCompare.
  ⚠ Правки CFO руками в Google-таблиці — видно після ≤15 хв або кнопкою «Оновити».
- opex.html: збережене (до доби) одразу + свіже у фоні (деталь і огляд); CFO — огляд паралельно зі списком
  локацій; журнал платежів у фоні одразу після даних; «Оновити» — nocache=1 і журнал заново. sw v7.341.

## v7.340 (лише фронт): OPEX — без попередження «у журналі більше, ніж факт»

Платежів у журналі більше за факт статті — нормально (Кругла: частина витрат списується на кухню в окремий
файл поза системою). Попередження прибрано: у такому разі вікно статті показує лише список платежів, без
порівняння з фактом. «Без підтвердження платежем: N» (факт > платежів) і «✅ повністю» лишаються. sw v7.340.

## v7.339: OPEX — сторінка самодостатня для директорки

Директорки нічого не вносять у файл OPEX (лише CFO), тож сторінка має замінити таблицю.
- Звірка перед змінами: сума статей у системі = рядок «Підсумок» (=SUM(B3:B31)) файлу, 12 міс × факт і бюджет,
  Осокорки / Голосієво / Кругла / Школа 228 — 100%; усі 25 статей є.
- Вікно статті: платежі місяця з «OPEX_Витрати_Лог» (лише «внесено», без «→Salary», «— не вносити —», пропущених),
  клік по місяцю; «без підтвердження платежем: N» (внесено вручну) або ⚠ «у журналі на N більше, ніж факт»
  (Кругла, вер.: оренда +6 485, комунальні +2 500); до серпня 2026 — «журнал ведеться з серпня 2026».
- Смуга «Дітей · Груп · Основного персоналу · OPEX на дитину в місяць» (+ «Знижки», якщо непорожні). Кількості —
  колонка «Бюджет» місяця (звірено: Осокорки вер. 63 / 6 / 17; третя колонка — незмінне базове число); для
  кварталу/року — середнє за місяці, що настали; на дитину — лише місяці з указаною кількістю. R&D — приховано.
- Бекенд: getOpexData.extras (сирі рядки за назвою); year ≠ поточний → NO_YEAR (досі поточний рік під чужим
  підписом). Архіву OPEX за минулі роки в системі немає — вибір року чекає файлів 2025 або січня 2027.
opex_cache_v2, sw v7.339.

## v7.338: нічний синк «Payment → картки» — і для «Управління»

`nightlySyncMissingKindergartens` (7:30) тепер з locScope 'kindergartens_mgmt' = Садочок + Управління
(Нац.Гвардії, Манхетен, Житомир). Школи — поза синком (v7.209). Кнопка «Синхронізувати відсутніх» — як була.
dryRun 25.09 (scope all, фільтр трьох локацій): Нац.Гвардії 0, Манхетен 0 (4 заведено вручну 24.09),
Житомир 1 — Поліщук Анастасія (mini Baby-ki, Payment р.100; вступний 6 000 у вересні, навчання з жовтня);
дубля немає (Поліщук Софія — інша дитина, Бігова). Садочки — 0. Скіпи синку: existing 756, dupInPay 0, idTaken 0.

## v7.337 (лише фронт): «Ліди» з'являються одразу з рештою меню

Для CFO «Ліди» видно на всіх 15 сторінках, але на 12 (analytics, categories, activities, control, invoices,
clients, reconcile, predmetnyky, salary_reconcile; salary/opex/tasks — трохи раніше) лише на load + 600 мс:
стартовий IIFE кликав applyRoleVisibility ДО того, як кінцевий скрипт її обгортав reveal() — пункт зникав і
повертався при кожному переході. Тепер reveal() (window._revealLeadsMenu) і список ролей (NAV_LEADS_ALLOW)
визначені одразу після пункту «Ліди» в меню й викликаються там же: роль із S.user, а до старту — із сесії в
localStorage. Кінцевий блок лише перезапускає reveal() після входу/зміни ролі. sw v7.337.

## v7.336: getClients — кеш, легкий режим, картки одразу

Заміри перед змінами: «1,7 МБ» — старий формат; сторінки вже беруть compact — 865 КБ, по мережі gzip ~100 КБ
(0,1–0,2 с); до першого байта 5,7–9,5 с, з них ~5 с старт Apps Script. «Відсутності (JSON)» — 51% compact.
Поля по сторінках: clients.html — усі 28 (картка зберігається з пам'яті — поля НЕ урізати); index.html — 5
(без «Статус» → _isCurrentKid рахував випускників/розірваних як поточних); meals.html — 4, тягнув усю мережу.
- бекенд: кеш list/roster (compact) шматками gzip+base64 по 90 000 символів, 10 хв; версія 'clients' — у
  saveClient, patchClientCell, deleteClient, mergeClientDuplicate, purgeAutoDraftCards, syncMissingClients;
  mode=roster (6 полів) + &loc=; diagClientsCompare — звірка;
- index.html: roster + status (кеш-ключ v3); meals.html: roster лише своєї локації (скидання при зміні локації);
- clients.html: картки/оплати/локації з браузера малюються одразу (крім медсестри), свіжі — перемальовують.
sw v7.336.

**Звірка й заміри v7.336 (прод).** diagClientsCompare: list 1 259 × 28, roster 1 259 × 6, розбіжностей roster↔list
0; 16 локацій, сума &loc= = 1 259, без локації 0; другий виклик — з кешу, кеш = прямий збір (побайтно).
Запити: list холодний 6,6–7,8 с → з кешу 5,7 с (ping 4,9 с); roster — 25 КБ по мережі замість 100 КБ;
roster &loc=Школа 228 — 2 КБ, 124 рядки (разом із випускниками; meals фільтрує active).

## v7.334: «Потребують уваги» — запит паралельно зі стартом, кеш, збережене одразу

Куди йшли ~25 с (відкриття з іншої сторінки, холодний кеш): стартові 5 завантажень clients.html паралельно —
15,4 с (найдовше getClients list, 1,7 МБ); розбір і малювання ~1–2 с; ПОТІМ по черзі getNeedsAttention 5,4–7 с
(бекенд ~1–2 с + старт Apps Script). Паралельне читання файлів локацій тут не застосовне — екран читає три
аркуші CRM-таблиці, а не файли локацій; кеш «по локаціях» теж — список рахується одним проходом.
- бекенд: кеш 10 хв (версії 'fill' + 'pay'), вузьке читання «Клієнтів» (6 колонок);
- clients.html: #needs_attention → getNeedsAttention стартує паралельно з initApp (_naEarlyStart); збережений
  список (до доби) видно одразу, без спінера; свіжий перемальовує; повторний рендер ≤60 с — без нового запиту.
sw v7.334.

**Звірка й заміри v7.334 (прод).** `diagNeedsAttentionCompare` двічі: identical=true, 53 = 53, byCat однаковий
(decide 23, departed 30). Але вузьке читання ПОВІЛЬНІШЕ: 2,7 / 3,2 с проти 1,5 / 2,1 с повного →
v7.335 повертає повне читання. Кеш: nocache 8,6 с (бекенд 3,1 с) → з кешу 5,6–6,1 с (старт Apps Script).

## v7.333: виклики без токена — дешевий лог, менше зайвих викликів, перелогін для недійсних токенів

Аналіз «Авторизація_Лог» 17–23.09 (1 815 рядків): ~850 — адмінські сесії з терміналу; ~500 — екран входу
(getUsers 244 — з v7.109 завжди PERM_DENIED, бо список лише CFO; getClosedMonths 255 — до входу);
~290 — люди з токеном, який бекенд НЕ приймає (saveAttendance зранку — директорки); 156 — GET без action
(два сплески, джерело невідоме). Лог пише й недійсні токени, не лише відсутні.
1. Бекенд: `_authLogMissing` — лічильник у кеші по годинах (мс), `_authLogFlush` дописує завершені години
   одним appendRows раз на годину; колонки Коли | action | method | причина | кількість.
2. index.html: живий getUsers з екрана входу прибрано (лишився MGMT_FALLBACK — звірено, 7/7 збігається).
3. getClosedMonths без токена не питаємо (index, clients, activities, predmetnyky); index — одразу після входу.
4. Недійсний надісланий токен → `authStale` першим полем відповіді → `_watch` на всіх 16 сторінках кидає
   на вхід ОДИН раз (токен запам'ятовується в mk_stale_tok — без зациклення).
sw v7.333.

## v7.332 (лише фронт): «Прізвище та ім'я» замість «ПІБ» для дитини

Дітям по батькові не пишемо — поле дитини перейменовано скрізь, де його видно: форма створення/картки
(підпис + підказка «Прізвище Ім'я» замість «Прізвище Ім'я По-батькові»), архів клієнтів, повідомлення
«Вкажіть прізвище та ім'я дитини», таблиця рахунків (і підпис на мобільному). Колонку «ПІБ дитини» в
таблиці «Клієнти» НЕ перейменовано — це ключ даних для бекенду й інших сторінок. «ПІБ мами/тата»,
співробітники, викладачі — без змін (дорослим по батькові пишуть). sw v7.332.

## v7.331: зведення ЗП — паралельне читання + кеш по локаціях

Було ~28 с удень: кеш v7.293 (5 хв, один на все зведення) скидався будь-яким експортом у Salary —
а сторінка предметників експортує після кожного уроку (debounce 2 с), тож удень майже завжди холодний
збір: 17 файлів по черзі ≈ 24 с + ~4 с старт Apps Script. Стало:
- `_salaryOverviewBuild('fast')` — усі файли одним `UrlFetchApp.fetchAll` → Sheets API values.get
  (UNFORMATTED_VALUE), сітка доводиться до форми getValues (≥80×37, '' у порожніх); файл, що не
  прочитався, — старим SpreadsheetApp (via: api / fallback);
- підрахунок сум — один код `_salaryOverviewLoc` для обох способів;
- кеш по локації 30 хв (ключ: глобальна версія + версія локації); `_salaryBump(loc)` у 9 місцях замість
  `_cacheBump('salary')`, без локації — скидає всі;
- salary.html: немає свіжого кешу, але є збережене за добу — показує одразу «дані на … · оновлюю…»;
- `diagSalaryOverviewCompare&year=` — старий і новий спосіб поруч, звірка по місяцях.
sw v7.331.

**Звірка й заміри (прод, 24.09 ~20:30).** `diagSalaryOverviewCompare`: 17/17 локацій × 12 місяців — факт і бюджет
старого й нового способу ідентичні; перший прогін усі `api-403` (не ввімкнено службу Google Sheets API — читалось
запасним шляхом), після ввімкнення служби — усі 17 `api`, знову ідентично. Рік-факт = знімку до змін.

| getSalaryOverview | Було | Стало |
|---|---|---|
| холодний (перечитати 17 файлів) | ~28 с (бекенд 24,2 с) | **7,0–7,5 с** (бекенд 2,0–2,2 с) |
| з кешу | 7,8 с (кеш жив ≤5 хв, скидався будь-яким експортом) | **3,9–7,0 с** (бекенд 0,3–0,4 с), кеш 30 хв по локації |

⚠️ Залежність: служба Apps Script «Google Sheets API» (Services → Sheets). Без неї — автоматично старий шлях
(via: fallback:api-403), суми ті самі, лише повільніше.

## v7.330: розбір рахунків точніший

Після v7.329 рахунок 30578.xls читався (1 285 символів), але дав лише ЄДРПОУ і суму 950, а постачальником —
«а, самовивозом…». Причини: номер шукався лише як «Рахунок №»/«Рахунок-фактура №» (не «Рахунок на оплату №»),
дата — лише цифрами, «Постачальник» знаходився всередині слова «постачальника». Виправлено всі три; назву
постачальника беремо з першої клітинки рядка і відрізаємо реквізити (ЄДРПОУ/ІПН/IBAN) замість відкидати
рядок. dryRun розпізнавання повертає textPreview (1 500 символів) для діагностики.

## v7.329: бот рахунків читає Excel; картки Манхетена

**Бот.** Рахунок 30578.xls (24.09, 3 спроби → «помилка»): Telegram віддає .xls як application/octet-stream,
бот імпортував усе в Google Документ з OCR і експортував text/plain; таблицю Drive в Документ не конвертує →
«Drive export HTTP 400: The requested conversion is not supported». Тепер .xls/.xlsx/.ods/.csv (за
розширенням або MIME) → Google Таблиця → export text/csv → `_invCsvToText` (клітинки через таб, коми в
лапках цілі — «2 450,00» не ламається, «10» і сума не зливаються). Ключ суми допускає «, грн» перед «:».

**Манхетен (дані, без коду).** Нічний автосинк Payment→CRM свідомо пропускає «Управління» (Благо, Житомир),
тож нові діти Манхетена карток не мали, а відмітки табеля йшли на неіснуючі ID. Заведено 4 картки у стилі
Манхетена (група з Payment, сума 0): Драган Артем, Франько Ростислав, Шиманський Олександр, Побуцька Марія.
ID = childId() → вересневі відмітки Драгана/Франька прив'язались без ремапу. Повний syncMissingClients
(scope all) НЕ запускати: завів би дублі 4 школярів Кар'єрної (картки під хостом) і 8 карток Школи Осокорки.

## v7.328 (лише фронт): бокове меню тримає активний пункт у видимій частині

Кожен перехід у меню — нове завантаження сторінки, тож сайдбар (position:fixed, overflow-y:auto) завжди
стартує з верху; прокрутку ніхто не зберігав. Відколи в HR-розділ додано «Картку локації» (v7.326 — CFO на
всіх сторінках, v7.327 — директорка), нижні пункти FINANCE («Додаткові» ≈1 135 px) опинились за краєм
екрана. Скрипт на всіх 15 сторінках: зберігає scrollTop меню перед переходом (sessionStorage), відновлює
після завантаження, а якщо активний пункт поза видимою частиною — центрує його; другий прохід через 700 мс
(після applyRoleVisibility), ручну прокрутку не перебиває. Перевірено headless Chrome 1280×800: без скрипта
активний пункт поза екраном, зі скриптом — по центру. sw v7.328.

## v7.327 (лише фронт): зайві Дашборд/Задачник і меню директорки

Правило «Дашборд лише CFO/HR, Задачник лише CFO» (v6.43) живе тільки в index.html — на інших 14 сторінках
CEO/HR/R&D бачили обидва пункти з матриці. Прибрано з самої матриці: 'dashboard' у ceo/hr_trainer/rnd_director,
'tasks' додається лише cco/legal. Директорка: 'loccard' тепер у матриці на всіх сторінках (досі push лише в index),
у salary_reconcile вирівняно needs_attention/cards_completion. Перевірено симуляцією сайдбару кожної сторінки:
CEO 16, COO 4, HR 9, R&D 12, директор 17 (+Харчування у Школі 228) + «Архів клієнтів» на сторінці клієнтів.
Бекенд не змінювався (v7.326). sw v7.327.

## v7.326: меню по ролях — CEO, COO, HR (L&D), R&D

Матриця PERMISSIONS (15 сторінок, 4 варіанти копій — правлено точково по ролях):
- CEO: −Потребують уваги, −Неповні картки, −Аналітика клієнтів/HR, −Зведена аналітика; +Картка локації → 16 пунктів.
- COO: лише Картка локації · Opex · Salary · Категорії (+'finance'); без Задачника; домашня — Картка локації.
- HR (hr_trainer): −Аналітика HR, −Додаткові, −Предметники; +Картка локації → 9 пунктів.
- R&D: −Аналітика HR; +Картка локації → 12 пунктів.
Верхні «Ліди / Контроль заповнення / Звірка платежів / Звірка ЗП» — новий `NAV_TOP_DENY` + `canSeeTopNet`
(NETWORK_ROLES не чіпали — він ще й дає доступ до всіх локацій); сторінки control/reconcile/leads/
salary_reconcile мають гард і для цих ролей ведуть на index. Картка локації: пункт додано в сайдбар усіх
сторінок (досі був лише на index); редагування для CEO/COO/HR/R&D уже дозволене (EMP_MGMT_ROLES).
Opex R&D: огляд по всіх локаціях тягне `getOpexOverview&category=Методична частина` (кеш розведено по
категорії), банер «Лише стаття… · немає статті: …». Директор/медсестра/вихователь — без змін. sw v7.326.

## v7.325: модель оплати предметників «Місяць»

Кол. I «Модель» у Предметники_Каталог: «За заняття» (порожньо = так само) або «Місяць» — тоді ставка
є місячною сумою. Експорт у Salary пише її як є, якщо за місяць було хоч одне заняття; без занять
клітинку не чіпає (ручна сума лишається), крім власного попереднього запису за журналом. Стеля норм і
перейменування рядка Salary для «Місяць» не застосовуються; старий експорт v6.5 такі позиції пропускає.
Сторінка предметників: «11 000₴ на місяць (фіксовано)», «N занять → 11 000₴».

Школа 228: Кримська 11 000/міс, Пахалюк (англ.) 20 500/міс; Маришина деактивована. Рядки Salary 36
(«англійска») і 44 («Фізкулькура») перейменовано renameSalaryRow — інакше експорт заводив дублі (P7).
Бекап Salary_BKP_2026-09-24_08-49-51. Нагадування: позиція зі ставкою 0 блокує відмітку уроку. sw v7.325.

## v7.324: Школа 228 — дві фізкультури й інформатика

Ставки каталогу: Німецька (Пахалюк) 330, Психолог (Хитрова) 500, Іспанська 330, Фізкультура Лакіза 720,
Фізкультура Баранов 590 (нова позиція), Кастомаха інформатика 577 (нова). Дві фізкультури — іменні
subject_norm «Фізкультура Лакіза»/«Фізкультура Баранов» (точні назви в CATALOG_TO_NORM_MAP, як v7.268),
бо з одним «Фізкультура» кожна позиція множила б ті самі уроки на свою ставку. Уроків фізкультури в
локації ще не було — сиріт немає. `_normalizeSubject` знає інформатику. Дзеркало в predmetnyky.html.
Заодно: картка Алексенко Стефанія (Школа 228, 1 клас, 17 000 з жовтня) — була лише в Payment. sw v7.324.

## v7.323: _aggregateOneLoc на спільному коді + Трембачов у винятках

`_aggregateOneLoc` (перезбір однієї локації після створення картки) була старою копією агрегатора:
не віддавала блок під-локації (24.09 о 12:55 школярі Кар'єрної стали двічі — у садку й у школі),
писала в «Група» нормалізований ключ і лише 17 колонок, тож кол. R «Група як у файлі» зсувалась
для всіх локацій нижче. Тепер рядки будує `_aggLocRows` — той самий код, що й `aggregatePayments`.
Хост і під-локації одного Payment-файлу перезбираються разом, нові рядки стають на місце старих,
пишуться всі 18 колонок; якщо рядків стало менше половини — агрегат не чіпається.

`getSchoolRoster` для під-локації бере й картки хоста з групою блоку (4 картки школи повернуто на
«Кар'єрна» / «Школа»). `_VAC_EXCEPTIONS` (+ дзеркало в clients.html) += Трембачов Герман.
sw cache v7.323.

## v7.322: оцінка співробітника — поквартально

Період оцінки — квартал, ключ `РРРР-Q1..Q4` у тому ж JSON кол. W (формат `{шаблон:{період:{…}}}` не змінився).
У картці вибір «Квартал»: поточний + 4 попередні. Старі помісячні `РРРР-ММ` (20 оцінок, 8 вихователів,
2026-01…03 і 2026-08) не мігруються — лишаються в списку як «Архів — помісячні», лише перегляд
(кнопки й «Зберегти» вимкнені, `saveAssessment` відмовляє).

Проти подвійного рахунку `getVyhovatelRatings`: якщо за квартал є квартальна оцінка, місячні того ж
кварталу в середнє не йдуть. Без квартальної — місячні рахуються як раніше. Дашборд: «оцінок: N»
замість «за N міс.». sw cache v7.322.

## v7.321: стаття й локація з рахунку — у рядок витрати

Після звірки директорка все одно обирала статтю руками. Тепер `matchInvoicesToPayments` віддає
`payEdrpou` (ЄДРПОУ **платежу**, не заявки), reconcile будує з нього той самий ключ групи
контрагента (`rcContractorKey`) і підставляє в рядок витрати **локацію** та **статтю** із заявки.

Стаття в підписі — вільний текст («вода», «канц»), статті OPEX — фіксований список файлу локації,
тож зіставляємо нормалізовано: точний збіг, далі ОДНОЗНАЧНИЙ префікс («вода»→«Вода»,
«канц»→«КАНЦТОВАРИ», «ремонт підлоги»→«РЕМОНТ»). Невідоме або неоднозначне НЕ підставляється —
хибна стаття гірша за порожню, бо її не видно оком у списку з 24 позицій; у бейджі видно
«·у рахунку «…», такої статті немає».

Ручний вибір не перебивається (`sel.category==null` — умова підстановки). Якщо один контрагент
має в цій виписці кілька рахунків з різними локацією/статтею — рядок витрати один, тож стаття не
ставиться взагалі, бейдж каже «·кілька рахунків, статтю не ставлю». Джерело статті видно бейджем
«·з рахунку». sw cache v7.321.

## v7.320: РАХУНКИ ЕТАПИ 5–6 — звірка з випискою і «✅ Оплачено»

`POST {action:'matchInvoicesToPayments', payments:[{date,amount,edrpou,counterparty,purpose,ref}],
dryRun, notify}`. Виписка на бекенд НЕ вантажиться — reconcile.html парсить її локально і шле лише
видаткові рядки.

**Правило матчу.** Сума (±0,01) — умова входу, але самої суми ЗАМАЛО: два рахунки одного
постачальника на однакову суму — звичайна річ, а хибний «оплачено» гірший за неоплачений. Потрібен
другий сигнал: ЄДРПОУ заявки == ЄДРПОУ платежу **або** номер рахунку в призначенні. Номер шукається
по ЦІЛИХ токенах (інакше «236» знаходився всередині «2360»), з урахуванням провідних нулів
(«СФ-0000123» ↔ «123»). Вікно дат: −5/+90 днів від дати рахунку. Неоднозначність в обидва боки
(кілька заявок на платіж або навпаки) = відмова, рядок іде в `ambiguous` для ручного рішення.

**Запис:** статус «оплачено», `дата оплати`, `matched_ref` = референс платежу, дописка в `лог`.
Тоді ж — ЄДИНЕ повідомлення бота в групу: «✅ Оплачено ДД.ММ» у тред до рахунку (див. v7.319).

**UI:** reconcile.html → режим «Витрати» → блок «🧾 Рахунки з Telegram»: «🔍 Звірити» (dryRun,
показує таблицю збігів і неоднозначних) → «✅ Позначити оплаченими і повідомити в групу».
sw cache v7.320.

**Перевірено dryRun на проді** (заявка №1, Тичини, ТОВ "Діана" 1 949,40 № 236):
збіг «ЄДРПОУ+№ у призначенні» ✅; чужий ЄДРПОУ без номера — 0; сума +10 коп. — 0;
платіж за 6 міс. до рахунку (поза вікном) — 0; «рах 2360» замість 236 без ЄДРПОУ — 0.
Допуск суми ±0,01 навмисний (float + округлення), тож платіж, що різниться на копійку,
вважається тим самим рахунком.

## v7.319: бот рахунків мовчить

За рішенням директорки бот НЕ пише в групу нічого, крім майбутнього «✅ Оплачено ДД.ММ» (етап 6).
Прибрано: відповідь «🧾 Рахунок №N прийнято» у вебхуку, рядок розпізнавання
«🔎 постачальник · ЄДРПОУ · сума» після черги, попередження про бракуючі локацію/статтю і про
неповне розпізнавання. Вітання в `tgInvoiceSetup` тепер лише за явним `silent:false`.

Усе це видно в листі «Рахунки_Бот»: статус (`новий` / `розпізнано` / `помилка`), `довіра`,
`лог` зі спробами і джерелом (`drive-ocr` / `drive+llm` / `llm`). Група лишається чистою —
там тільки рахунки від директорок.

## v7.318: сміття з OCR не перекриває LLM

Перший прогін по живому фото (рахунок №1) з працюючим Drive-OCR (756 символів) показав три вади
regex на сканованому тексті:
1. «Постачальник:» у шапці реквізитів → значенням ставала ШАПКА ТАБЛИЦІ
   («код ЄДРПОУ Р/рахунок No Банк:»);
2. OCR читає «№» як «No», і з «Р/рахунок No Банк» виходив номер рахунку «o»;
3. перше (сміттєве) входження блокувало справжнє «Рахунок No 236» — `match` без `/g`.

Виправлено: `_invSaneSupplier` відсіює рядки зі словами реквізитів і без трьох літер поспіль;
номер зобов'язаний містити цифру, а всі входження перебираються до першого валідного; при злитті
з LLM значення regex беруться ЛИШЕ якщо пройшли перевірку (ЄДРПОУ 8–10 цифр, сума > 0, дата у
форматі ДД.ММ.РРРР) — інакше перемагає LLM, який бачить структуру документа.

На тому самому OCR-тексті тепер: ТОВ "Діана" · 19017612 · № 236 · 1 949,40 · 22.09.2026 · high.

## v7.317: Drive-текст через export, не DocumentApp

Перший бойовий прогін (рахунок №1, фото 134 КБ): `DocumentApp.openById` → «немає дозволу…
Потрібні дозволи: .../auth/documents». Вебзастосунок авторизований зі старим набором скоупів, а
новий скоуп вимагав би переавторизації і перевидання деплою. Тепер текст конвертованого Doc
береться `GET /drive/v3/files/<id>/export?mimeType=text/plain` під `ScriptApp.getOAuthToken()` —
drive-скоуп уже є від Advanced Drive Service. `DocumentApp` лишився резервом.

Наслідок першого прогону: Drive-гілка мовчки віддавала порожній текст, і КОЖЕН рахунок ішов у LLM
(результат правильний, але це гроші за те, що безкоштовна гілка мала зробити сама).

## v7.316: РАХУНКИ ЕТАП 3 — розпізнавання файлу

Черга: заявки зі статусом «новий» → `getFile` у Telegram → текст → `ЄДРПОУ / № / сума / дата /
постачальник` → статус «розпізнано». Тригер `invoiceQueueTick` раз на 5 хв, до 5 заявок за прогін
(скачування + OCR + LLM не влазять ані у вікно відповіді Telegram, ані завжди в 6 хв Apps Script).

- **Текст:** Drive-конвертація у Google Doc з OCR (`ocrLanguage:'uk'`), підтримано і v3
  (`Drive.Files.create`), і v2 (`Drive.Files.insert`) — версія Advanced Service залежить від того,
  що ввімкнули в редакторі. Тимчасовий Doc одразу в кошик.
- **Regex:** «Рахунок-фактура № СФ-0000123 від 20.09.2026», «Код ЄДРПОУ 32154789», «Всього до
  сплати: 2 450,00». ⚠️ У числових класах ЛИШЕ пробіл і `\u00A0`, без `\s`: `\s` включає `\n`, і
  шаблон склеював числа з сусідніх рядків («№ 5» + «1 200,50» → 51 200,50). Знайдено на тестах.
- **Фолбек Claude API** (vision) — коли regex не дав ЄДРПОУ або суму. Ключ `ANTHROPIC_API_KEY` у
  Script Properties; без ключа фолбек просто не вмикається. Модель `claude-opus-5`, override —
  `INVOICE_LLM_MODEL`. Ліміт файлу 4 МБ. Просимо чистий JSON і беремо перший `{...}` — не залежимо
  від форми structured-outputs API.
- **Довіра:** high (є ЄДРПОУ+сума+номер), medium (2 з 3), low. У тред рахунку йде друга відповідь:
  «🔎 ТОВ «Аква-Сервіс» · ЄДРПОУ 32154789 · № СФ-0000123 · від 20.09.2026 · **2 450,00 грн**»;
  при low/medium — приписка «перевірте».
- **Колонка «спроб»** (append-only): 3 невдачі → статус «помилка», щоб черга не крутилась вічно.
- Роути: `POST {action:'processInvoiceQueue', dryRun}` (dryRun читає файл і показує розпізнане,
  нічого не пише і в групу не відповідає), `POST {action:'ensureInvoiceTrigger'[, remove:true]}`.

## v7.315: підпис рахунку без роздільника («Тичини вода»)

Перша бойова заявка (22.09, Тичини) прийшла з підписом **«Тичини вода»** — без «/». Локація
вгадалась, а стаття лишилась порожньою: ціле вікно «Тичини вода» підходило під ПРЕФІКС «Тичини»,
тож слово «вода» не потрапляло в залишок. Тепер `_invMatchLocInPart` шукає локацію як послідовність
слів у два проходи — спершу ТОЧНІ збіги по всіх вікнах (найдовше вперед, щоб «Манхетен (Благо)» не
зрізався до «Манхетен»), і лише потім префіксні. Решта слів іде в статтю, порядок частин вільний.

Перевірено: «Тичини вода» → Тичини + вода; «вода Тичини» → Тичини + вода; «Манхетен (Благо) ремонт»
→ Манхетен (Благо) + ремонт; «Кругла кухня продукти» → Кругла + «кухня продукти»; «Школа ремонт» →
локація порожня, у логу обидві школи.

## 🔴 Релей без `?s=` — апдейт зникає без сліду (22.09.2026)

Вебхук рахунків зареєстрували як `https://<worker>/invoice` **без** `?s=<секрет>`. Наслідок підступний:
Telegram доставляє (`pending 0`, `last_error` порожній), воркер віддає 200, а Apps Script звіряє
`e.parameter.s` із `INVOICE_WEBHOOK_SECRET`, не бачить його і відповідає `bad secret` — заявка не
пишеться, у `TG_Err` теж нічого. Зовні виглядає як «бот не бачить повідомлень».

Виправлено з двох боків: воркер бере секрет із query **або** із заголовка
`X-Telegram-Bot-Api-Secret-Token` (його Telegram шле сам, бо `setWebhook` передає `secret_token`);
`tgInvoiceSetWebhook` (v7.314) дописує `?s=` до `hookUrl`, якщо його там немає.

Діагностика на майбутнє: `pending 0` + порожній лист = апдейт дійшов і був відкинутий; перевіряти
секрет, а не доставку. POST на `/invoice` без тіла → 200 `ok` означає, що `EXEC_URL` у воркері задана
(інакше 500).

## 🔴 Telegram не приймає вебхук просто на Apps Script (302) — потрібен релей

Бот рахунків: `setWebhook` на `exec?action=tgInvoiceWebhook&s=…` пройшов (`Webhook was set`), але
`getWebhookInfo` показав `last_error: "Wrong response from the webhook: 302 Found"`, `pending: 1`.
Apps Script web app на POST завжди відповідає 302-редіректом на `script.googleusercontent.com`, а
Telegram за редіректами не йде — вважає доставку невдалою і ретраїть. Це та сама природа, що й
[[apps-script-post-returns-html]] з боку фронту, але наслідок гірший: апдейт не доходить зовсім.

Рішення — Cloudflare Worker-релей: `_worker/tg-relay.js` (у git, деплоїться вручну в Cloudflare).
Маршрути `/invoice` → `tgInvoiceWebhook`, `/leads` → `tgWebhook`; змінна `EXEC_URL`. Воркер віддає
Telegram 200 одразу, а запит до `exec` доробляє у `waitUntil` (холодний Apps Script буває повільним).
Далі `tgInvoiceSetWebhook {hookUrl:'https://<worker>/invoice?s=<секрет>'}` — параметр `hookUrl` у коді
був саме під це.

⚠️ Відкрите: як доставляються апдейти БОТА ЛІДІВ. Він працює, отже його вебхук майже напевно вже
стоїть на якомусь релеї — URL у репозиторії не зафіксований. Перевірити в Cloudflare і записати сюди;
до того часу вебхук лідів НЕ чіпати.

## v7.313: діагностика токена бота рахунків

`tgInvoiceSetup` на продіv7.312 віддав «chat_id не знайдено», а справжня причина була в
`telegram:"Unauthorized"` — Telegram не прийняв `INVOICE_BOT_TOKEN`, тому і `getUpdates` нічого не
повернув. Тепер setup починає з `getMe` і на відмові каже прямо: `bot_id` (частина токена до
двокрапки — не секрет), довжина токена, чи є в ньому пробіл. Успішний setup повертає `@username` бота.

## v7.312: БОТ РАХУНКІВ НА ОПЛАТУ — етапи 0–2

Окремий Telegram-бот на тій самій інфраструктурі, що й бот лідів (вебхук просто на Apps Script
`exec`, без проксі). Жодна функція бота лідів не змінена.

- **Properties:** `INVOICE_BOT_TOKEN` (ставить користувачка), `INVOICE_CHAT_ID` і
  `INVOICE_WEBHOOK_SECRET` (створює `tgInvoiceSetup`; chat_id бере з `getUpdates`, доки вебхук
  ще не стоїть — звідси порядок: setup → setWebhook).
- **Лист «Рахунки_Бот»** у CONFIG-таблиці, 21 колонка; `file_id`, `ЄДРПОУ`, `№ рахунку` — текстовий
  формат (інакше Sheets зріже нулі ЄДРПОУ і перетворить file_id на експоненту).
- **Роути:** `POST ?action=tgInvoiceWebhook&s=<secret>` (повз `_authGate`, як у лідів),
  `POST {action:'tgInvoiceSetup'|'tgInvoiceSetWebhook'}`, `GET ?action=getInvoiceRequests`,
  `GET ?action=invoiceBotDryRun`.
- **Вебхук тонкий:** приймає лише свою групу, дедуп `update_id` зі СВОЇМ префіксом кешу (`invu_`),
  пише рядок і виходить. Файл не качається — етап 3 візьме його за `file_id`. Причина: Telegram
  ретраїть апдейт, якщо не отримав 200 швидко, а скачування + OCR у ці секунди не влазять.
  `allowed_updates:['message','edited_message']` — фото і документи приходять БЕЗ `text`.
- **Підпис «стаття / локація»:** роздільники `/ \ · — – , перенос`, порядок частин будь-який;
  локація — за реєстром CONFIG, з фолдом гомогліфів (латинська «a» в «Круглa» ловиться).
  Неоднозначний префікс («Школа» → 228 / Осокорки) локацію НЕ ставить, пише варіанти в лог:
  рахунок у чужу локацію гірший за порожню клітинку.
- **Відповідь у тред** — одна, на сам рахунок: «🧾 Рахунок №N прийнято · локація · стаття» і, за
  потреби, чого бракує. На звичайні повідомлення групи бот мовчить.

Далі: етап 3 (тригер: `getFile` → Drive OCR/конвертація → regex, фолбек Claude API),
етап 5 (звірка з випискою в reconcile за ЄДРПОУ + сума + № рахунку), етап 6 («✅ Оплачено ДД.ММ»).

## v7.311: предметники — дубль уроку і норма за датами

Тичини, Find-iki, Англійська, вересень: 8 занять у сітці, лічильник **10/10**, решта днів
заблокована. Причина — два дублі рядків (08.09: id 3244+3247; 10.09: 3367+3371), бо:
- `savePredmetnykyLesson`/`bulkPredmetnykyLessons` писали рядок **без перевірки**, чи урок на
  (локація, група, предмет, дата) вже є;
- ліміт рахував **рядки**, а не унікальні дати (`countLessonDatesInRow` на фронті — теж, попри назву).
Механізм появи: POST Apps Script віддає HTML, фронт вважає це збоєм і відкочує галочку, хоча
рядок записався → вихователька клікає вдруге (id дублів завжди через кілька номерів від оригіналу —
різні пакети, не один).

Виправлено: обидва write-шляхи віддають `{ok:true, dup:true, id}` замість другого рядка; норма
рахується за унікальними датами (бекенд `_predDayKey` + `daySetFor`, фронт — `countLessonDatesInRow`);
у черзі `_pqFlush` — фільтр однакових позицій у межах пакета. sw cache v7.311.

Скан вересня (усі 14 локацій, 1113 уроків) — 4 дублі: Тичини Find-iki Англійська 08.09 і 10.09,
Бровари 16.09 Англійська (Find-iki 3655+3658 і «miniBaby-ki Сніжана» 3656+3659).

## v7.310: абонементні заняття «Чомусики» / «Мама+я» (activities.html + бекенд)

Діти цих груп ходять лише на одне заняття; абонемент клієнта — руками в «Бюджет навчання»
(ціна клієнту в каталозі 0, «Бюджет доп» не чіпається); викладач — «За заняття» через
`exportToSalaryExtras`. Конвенція **назва групи = назва заняття** (нормалізовано, без пробілів і регістру):
- **Бекенд:** `SUBSCRIPTION_ACTIVITIES = {чомусики:8, 'мама+я':8}`; `getActivitiesCatalog` віддає `subLimit`;
  `addAttendanceMark`/`bulkAttendanceMarks` → `SUB_LIMIT` при ≥8 відмітках дитини на це заняття за
  календарний місяць (рахує і в межах пакета). POST-маршрут `renameSalaryRow` (функція v7.195).
- **Фронт:** група з такою назвою → випадайка «Заняття» ставиться на нього і `disabled`; у сітці місяця
  бейдж X/8 біля дитини (червоний на 8), вільні клітинки після 8-ї неактивні; клік понад ліміт — тост.
  sw cache v7.310.
- **Дані (після деплою):** каталог +3 (Бігова «Чомусики» 350/За заняття; Манхетен і Нац.Гвардії «Мама+я»
  За заняття, ставка 0 — Іра проставить); Salary Нац.Гвардії р.62 «Мама і Я» → «Мама+я» (секція
  extras, щоб експорт не створив другий рядок). Бігова р.30 «Вихователь Чомусики 350» (main) і Манхетен
  р.45 «МАМА +Я 370» (subjects) не в секції додаткових — лишаються історією, P7 створить нові рядки.
  Кругла не чіпається. Модуль v7.23 (`Чомусики_Відвідуваність`, predmetnyky) — 0 відміток, лишено.

## Бекенд v7.309: renameActivity + addActivity дубль за назвою

- **addActivity** ловив дубль ключем `_attDupKey(loc, модель, ставка)` — функцією для відміток, без
  назви заняття. Два гуртки з однаковою моделлю і ставкою вважались одним: «Вокал» 350/За заняття у
  Позняках тихо повертав id «Англійська групові» з `{ok:true, dup:true}`. Тепер дубль = локація +
  назва (нормалізовано, `_journalNormName`); у відповіді dup є `name` наявного рядка.
- **renameActivity** `POST {loc, oldName, newName, dryRun}` — перейменування заняття наскрізь:
  Додаткові_Каталог (кол. C), Додаткові_Відвідуваність (кол. G, знімок назви), Додаткові_Обʼєднання
  (кол. D), рядок Salary у секції «Додаткові заняття» (через `renameSalaryRow`: guard expectName +
  copyTo-бекап). Відмова, якщо в каталозі локації вже є нова назва (це злиття). dryRun за замовч.
  Кейс: Оранж «Гімнастика» → «Акробатика» (каталог id 47; 68 відміток чер–сер 2026 по 380;
  Salary рядок 43; обʼєднань 0). Ставки в каталозі не чіпались.
- Тичини, Тюріна Аміна: «(доп)» лишилось лише в колонці «Ім'я дитини» Табеля (7 рядків, ID
  `c_Тюріна Аміна (доп)_Тичини` = ID картки, не сирота). Відмітки додаткових і Payment уже
  «Тюріна Аміна», експорт матчить рядок 18, журнал синхронний. Лікування — `remapAttendanceId`
  (fromId=toId, newName), ID картки не змінюється.

## Фронт v7.308: номер договору XX-XX-XX або XX-XX-XXX (clients.html)

Маска поля «Номер договору» пропускала рівно 6 цифр і зрізала тризначний хвіст Тичин
(18-25-264 → 18-25-26) — звідси «новий формат» і дублі у картках. Тепер третя група 2–3 цифри
(`maxlength=9`), бекенд не змінювався (номер — текст, ніде не парситься). Додано підказку
«⚠️ Такий номер уже є: …» під полем і confirm при збереженні дубля в межах локації (не блок —
близнюки). sw.js cache v7.308. Дані Тичин не чіпались: 23 × XX-XX-XXX, 9 × XX-XX-XX, 6 порожніх,
4 зламані (Вакофян Діана «18», Боденчук «18-24», Нестеренко «18-24-1989», Кошик «182-24-229»),
дублі 18-22-190 ×4 і 18-25-24 ×2 — з директоркою руками.

## Каталог додаткових: правка за id + локація (v7.307)

«Додаткові_Каталог» має фантомну локацію «Осокорки сад» — 11 рядків з id 48–58, тими самими, що
у справжніх «Осокорки». `updateActivity(id)` брав перший рядок за id → правки Осокорків лягали у
фантом, справжні рядки не змінювались, а `deleteActivity` (м'яке `active:false`) «видаляв» теж
фантом. Тепер `updateActivity`/`deleteActivity` приймають `onlyLoc`; `purgeActivitiesByLoc`
фізично прибирає рядки локації, якої немає в реєстрі і на яку немає відміток (dryRun, `YES_PURGE_LOC`,
знімок `_purgeloc`). Кейс Осокорки 18.09: відмітки й Payment перераховано (302 відмітки, +30 720;
Бюджет-доп жовтень 96 220 → 128 060), Salary додаткових чекає правки каталогу.

## Переоцінка відміток за місяць (v7.306)

`POST {action:'repriceAttendanceMarks', loc, activityName, year, month, oldPrice, newPrice, expected?, dryRun}` —
той самий `repriceAttendanceMarks` (v7.188), тепер доступний як маршрут і з режимом «весь місяць»
(`year+month` замість `date`). Ціна вморожується у відмітку при проставлянні; правка каталогу
старі відмітки не змінює — тільки цей маршрут, після нього `exportAttendanceToPayments`.
Кейс: Бігова, вересень 2026 — Лего 260→340, Футбол 270→330, Логопед 450→510.

## Salary-рядки з каталогу автоматично (v7.305)

1. **exportToSalaryExtras — P7.** Заняття з відмітками за місяць, для якого в секції «Додаткові
   заняття» рядка немає → рядок створюється з назвою як у каталозі, Budget(N+1) = факт. Місце —
   ПЕРЕД останнім рядком секції (всередину `SUM(...)` підсумку); без секції — у кінець аркуша із
   заголовком. Без відміток — рядок не створюється (лише `not-in-salary` у details).
2. **Предметники — перейменування під ставку.** Рядок знайдено за старою ставкою (P3/P5 — число в
   назві ≠ ставці каталогу; P6 — числа немає) → назва переписується (`_predRenameForRate`:
   «Логопед 250» → «Логопед 300», «Хореограф 270 (Оля)» → «Хореограф 300 (Оля)»), суми не чіпаються.
3. **План у Budget(N+2)** = одиниці місяця N × ставка каталогу — ЛИШЕ в порожню клітинку без формули
   (наступний експорт перезапише фактом). `planNext:false` вимикає. Через межу року не пишеться.
   Припущення: «бюджет наступного місяця з каталогу» = план на місяць після того, куди йде факт.
Обидва експорти повертають `rowsAdded`, `renamed[]`, `planMonth`, `planned`; `dryRun:true` показує все без запису.

## Порожні заголовки блоків ≠ безіменні діти (v7.304)

Після v7.303 Бровари (+61 875 навч) і Кругла (+28 499) стали БІЛЬШІ за CF: у файлах є блоки
з порожнім заголовком (Бровари р.67, Кругла р.103) — без назви, але з `=SUM(` по блоку. За
значеннями це «рядок без імені з грошима», тож парсер брав підсумок блоку як дитину.
`_paymentSumHeaderRows(sheet, n)` читає формули колонки B; такі рядки пропускають і річний
агрегат, і `dryRunNamelessRows`. Решта 4 садки з CF сходились до гривні вже на v7.303.

## Безіменні рядки → річний агрегат як вибулі (v7.303)

Сканування v7.302: 305 рядків без ПІБ із сумами, 30,85 млн факту за січень–серпень (вибулі
діти зі стертим ім'ям). `parsePaymentSheet(…, keepDeparted)` тепер віддає такий рядок дитиною
«(без імені, р.N)» з `departed:true` у своїй групі; `aggregatePaymentsYearly` пише його зі
своїми сумами і «Вибув»='так'. Ростерні читачі та фронт уже пропускають «Вибув» (v7.299).
Місячний «Оплати», табель, рахунки — без змін. Після деплою — `runAggregateYearly`:
очікувано ≈1066+305 рядків, Σ факт Січ–Сер +30,85 млн.

## Безіменні рядки з грошима (v7.302)

`GET ?action=dryRunNamelessRows[&loc=]` — read-only: рядки Payment без ПІБ або з числовим «ім'ям»
(слот), у яких є суми; блок, рядок, помісячно факт навч/вступ/доп і бюджети. CF бачить їх через
SUM блоку, «Оплати-Рік» — ні (парсер пропускає порожнє ім'я).

## Звужені підсумки заголовків Payment (v7.301)

Прочитано CF/PL/Payment шести садків: CF бере заголовкові рядки Payment (`C8=Payment!B25`),
PL — CF + ті самі заголовки. Підсумок блоку — `=SUM(B26:B45)` у заголовку Payment; `moveRows`
звузив початок діапазону там, де перенесені діти стояли першими після заголовка, а
повернення перед початком діапазону його не розширює. 8 заголовків (Бровари р.25, Голосієво
р.26, Бігова р.67, Кругла р.4, Оранж р.4 і р.25, Осокорки р.4 і р.25), 571 клітинка.
`setLocSheetFormulas` на Payment тепер дозволяє лише `=SUM(` у заголовкових рядках (колонка A —
заголовок або порожня); рядки дітей — відмова. Знімок вкладки `_sumfix` перед записом.

## CF/PL читають Payment фіксованими діапазонами (v7.300)

CF і PL — вкладки в тому самому файлі локації, що й Payment. Формули з фіксованими діапазонами
рядків Payment; `moveRows` у «Вибули» (14–15.09) звузив діапазони слідом за клітинками, а
повернення рядків (v7.297) їх не розширило → CF не бачить повернутих дітей (Бровари
«Preschool 2 Ольга»: −5 дітей, Q1 −225 000 / Q2 −243 350).
- `GET ?action=getLocSheetFormulas&loc=…&tab=CF` — read-only: усі формули вкладки + колонка A
  Payment з номерами рядків і ознакою заголовка.
- `POST {action:'setLocSheetFormulas', loc, tab, edits:[{a1,formula}], dryRun:false, confirm:'YES_SET_FORMULAS'}`
  — точковий запис; знімок вкладки `_cffix` перед записом; Payment не чіпає.

## Блоки «…вибули» — у гроші, не в ростер (v7.299)

`parsePaymentSheet(…, keepDeparted=true)` віддає службовий блок групою `departed:true`
(group = заголовок як у файлі). Лише `aggregatePaymentsYearly` вмикає це: рядки пишуться
в «Оплати-Рік» зі своїми сумами + нова остання колонка **«Вибув»='так'** (після «Оновлено»,
існуючі колонки не зсуваються). CF/PL, що сумують по Локація+місяць, підхоплюють автоматично;
за потреби відділити — фільтр по «Вибув».

Ростерні читачі пропускають такі рядки: `getInvoiceListData`, `_invoiceSumFromYearly`,
`syncMissingClients` (автосинк карток), знімок `_PaymentRoster`, `syncGroupsFromPayments`;
фронт — `_yearlyRowVisible`/`buildGroupsFromYearly` (clients.html), «дітей/міс» на дашборді
(index.html). Місячний «Оплати» і всі екрани ростера — без змін (блок далі ігнорується).
Кейс: Житомир «Preschool вибули», 13 дітей, ~1,49 млн за січень–серпень. Після деплою —
`runAggregateYearly`, фронт — git push (sw.js cache v7.299).

## Річний агрегат: гроші з рядка парсера, не «за ПІБ» (v7.298)

`aggregatePaymentsYearly` брав суми через `nameToRow[ПІБ]` = останній рядок з таким ПІБ у файлі.
Дві картки одного ПІБ (Драган Артем, Манхетен: «Мама+я» 11 065 і «Розумники» 135 245) обидві
отримували суми останнього рядка. Тепер `parsePaymentSheet` віддає `row`, агрегат читає саме його.
Після деплою — `runAggregateYearly`.

**Відкат 17.09.2026 виконано (v7.297):** 101 рядок у 13 файлах повернуто на місця, порожні
заголовки «Вибули» прибрано, знімки `BKP_Payment_20260917_13xxxx_undomove`. «Оплати-Рік»
952 → 1053 рядків, факт січень–вересень +10,87 млн. Житомир «Preschool вибули» (13) не чіпали.

## Скасування переносів у блок «Вибули» (v7.297)

Перенесення `movePaymentRowsToBlock` (14–15.09) прибрало з «Оплати-Рік» і «Оплати» 113 рядків
і ~12,4 млн факту за січень–вересень — CF/PL, що читають агрегат, занижені на 11–13 % у
січні–червні. Правило відтепер: **Payment не чіпаємо**, дитина зникає з ростера статусом картки.

- `GET ?action=dryRunUndoMoveBlock[&loc=]` — read-only: по кожному файлу найстаріший знімок
  `BKP_Payment_*_moveblock`, які рядки повернуться, з якого рядка в який, під який заголовок, ₴.
- `POST {action:'undoPaymentMoveBlock', loc, dryRun:false, confirm:'YES_UNDO'}` — точковий
  `moveRows` за планом (не повний відкат аркуша — щоб не затерти оплати після 15.09), потім
  прибирає порожній заголовок «Вибули» + роздільник, якщо їх у знімку не було. Знімок перед
  записом: тег `undomove`. Після серії — `runAggregate` і `runAggregateYearly`.
- Житомирський «Preschool вибули» (13 дітей, існував до інструмента) не рухається — його
  гроші повернуться в агрегат лише правкою парсера (окрема задача).

## Стеля предметників — за блоком того місяця, що перевіряється (v7.296)

`_predCeilingFor(loc, subject, group, ymd)` → `_predNormLookup(…, ymd)` →
`_loadPredNorms(ymd)`: без `ymd` — сьогоднішня дата, як досі. Передано скрізь, де
місяць відомий: `diagPredNorms` (місяць запиту), `savePredmetnykyLesson` і
`bulkPredmetnykyLessons` (місяць дати уроку), `exportPredmetnykyToSalary` (місяць
експорту). Наслідок: серпнева діагностика показує серпневі перевищення за I–M, а
не 10 фантомних за C–G; відмітка уроку заднім числом (коли місяць відкритий)
звіряється зі стелею свого місяця. Mock: Find серпень 12 / вересень 10; Study
серпень 12 / вересень 15. Норми локації (`_loadPredLocNorms`) від блоків не
залежать. cache v7.296.

## Норми предметників: з вересня діє блок C–G (v7.295)

Лист «Норми» має два блоки: C–G (кол. 2–6) і I–M (кол. 8–12). v7.230 вмикала з
01.09.2026 блок I–M, а мало бути навпаки. Що діяло і що стане (`diagPredNorms`, 16.09):

| Регіон | Предмет | Було (I–M) mini/Baby/Find/Study/Pre | Стане (C–G) |
|---|---|---|---|
| Київ, Львів | Англійська | 8 / 8 / **12** / **12** / **12** | 8 / 8 / **10** / **15** / **15** |
| Київ | Музика, Хореограф | 6 / 6 / 6 / 6 / 6 | без змін |
| Київ | Логопед | 0 / 4 / 4 / 4 / 4 | без змін |
| Київ | Психолог | 0 / 2 / 2 / 2 / 2 | без змін |
| Львів | Музика 8, Хореограф 4, Логопед 0/4 | | без змін |
| Київ | Німецька, Іспанська, Фізкультура, Архітектура, Фітнес, Speaking club | I–M порожній → 0 | C–G = 0 (без ліміту) |

Змінюється лише Англійська: Find 12 → 10 (жорсткіше), Study/Preschool 12 → 15
(мʼякше). Норми рівня локації («Манхетен», «Нац.Гвардії», 27 рядків) і школи
не залежать від блоків. Перевищень за вересень **немає ні за I–M, ні за C–G**:
найбільше дат в Англійській — 9 (Study/Preschool у Тичині, Борщагівці, Житомирі),
у Find-групах — 7 (стеля 10).

Код: `_predNormsUseCG(ymd)` (≥ `PRED_NORMS_CG_FROM` = 2026-09-01 → C–G; раніше →
I–M, порожня клітинка I–M → C–G); `_predPickNorm(row, useCG, imIdx, cgIdx)`;
`diagPredNorms` віддає `activeBlock` і поля `cg`/`im`. Уроки, відмітки, експорти —
без змін: норма лише стеля (`_predCeilingFor`) і підказка NORM_REACHED при відмітці.
Кеш `getPredmetnyky` (5 хв) підхопить нові норми сам. cache v7.295.

Перевірено на проді після деплою: `diagPredNorms&month=9` → `activeBlock: C-G`,
240 комбінацій, перевищень 0; `month=8` → `activeBlock: I-M`. Нюанс діагностики:
стелю для `over` вона рахує через `_predCeilingFor` за сьогоднішньою датою (C–G),
тому для серпня показує 10 «перевищень» Англійської за вересневими нормами — це
не збій даних і на серпневий експорт (уже виконаний) не впливає.

## getErrLog — журнал помилок бекенду видно без запиту (v7.294)

Лист `TG_Err` (куди пише `_tgErr`) не мав read-only маршруту; збої маршрутів doGet/
doPost губились у відповіді `{ok:false}`, а справжні збої нічної гарантії лишались у
Script Properties. Коли гарантія падала кілька днів, ніхто не бачив.

- `getErrLog&days=7&actorId=` (GET, **лише CFO/HR** або id 1 — роль з токена або
  actorId): записи TG_Err за N днів (новіші перші, до 300), `byWhere`, `byKind`
  (system / bot / data / other), `byDay`, стан гарантії (`getExportGuardStatus`) і
  `guardAlert` — «зависла» (>45 хв у роботі), «не завершувалась >30 год», «жодного разу».
- doGet/doPost: виняток маршруту → `_tgErr('route:<action>', err)`.
- Нічна гарантія: справжні збої прогону (усе, крім NO_MARKS) → `_tgErr('nightly-guard', …)`.
- `control.html` («Контроль заповнення»): розділ «Помилки бекенду за 7 днів» для
  CFO/HR — банер `guardAlert`, чипи прогону гарантії (фініш, локації, клітинки, збої),
  розбивка за типом і таблиця останніх 40 записів. cache v7.294.

Що в листі (прод, 16.09, `actorId=1`; без actorId → PERM_DENIED):
- **За 7 днів — 0 записів.** Останній запис узагалі — 07.09 17:52.
- За 30 днів — 1 173: **1 161 `attendance-orphan`** (майже всі — STAFF:: хибна тривога,
  зупинена v7.257; останні 07.09), 11 `cb`/`cb-res` (колбеки бота, 18.08), 1 `mirror-newtab`.
- **Справжнє серед сиріт** (16 записів без STAFF::): Позняки — «Драгунов Іван» і
  «Бабанін Мирон» (відмітки 31.08–07.09 від медсестри й директорки на ID, якого немає
  серед карток), Голосієво — «Лук'янченко Данило» (ID картки каже «Лук'янчук» — з
  аудиту, 24 ID ≠ ПІБ). Тобто три дитини відмічаються, а в картках їх під цим ID нема.
- Гарантія: фініш 16.09 06:17 UTC, 17/17, Payment 20 кліт., Salary 7 кліт., 7 NO_MARKS.
- Чому «чотирьох днів падіння» в листі не видно: до v7.294 гарантія й маршрути в TG_Err
  не писали взагалі — це і виправлено; наступний збій буде видно в «Контролі».

## Кеш 5 хв для getSalaryOverview і getFillStatus (v7.293)

Обидва були близько до ліміту 6 хв: `getSalaryOverview` відкриває 17 Salary-файлів
підряд (45–68 с), `getFillStatus` читав 50 тис. рядків Табеля × 8 колонок (61–62 с).

- `_cacheGzGet/_cacheGzPut` — відповідь у CacheService gzip+base64 на 300 с; ключ
  включає версію групи `_cacheVer('salary'|'fill')`; `nocache=1` обходить.
- **`ver_salary`** bump-ають: `exportToSalaryExtras` (реальний запис зі зміненими
  клітинками), `exportPredmetnykyToSalary`, `exportPredmetnyToSalary`,
  `salaryReconcileApply`, `salaryAddExtrasPayments`, `addSalaryRow`, `deleteSalaryRow`,
  `cashPayoutSheet`. Ручні правки у файлах — покриває TTL.
- **`ver_fill`** bump-ають: `saveAttendance`, `deleteAttendanceRecord`, `dedupAttendanceApi`,
  `saveClient`, `deleteClient`, `patchClientCell`, `mergeClientDuplicate`,
  `purgeAutoDraftCards`, `syncMissingClientsFromPayments`, `saveEmployee`, `deleteEmployee`.
- `getSalaryOverview` кешується лише без `errors` (щоб тимчасовий збій відкриття файлу
  не завис на 5 хв). У відповіді `cached`, `cachedAt`, `ms`.
- Бонус для холодного шляху: `_fillReadAttendance` читає Табель через індекс дат
  (v7.289–290) — лише сегменти потрібних днів, 8 колонок; будь-який збій → старий
  шлях (50 тис. рядків).

Заміри ДО (прод v7.292, 16.09): `getSalaryOverview&year=2026` 45,5 с (10 КБ);
`getFillStatus` 1–15.09 — 62,5 с (36 КБ); день 15.09 — 60,9 с (19 КБ).
Заміри ПІСЛЯ (прод v7.293, 16.09 09:15 Київ — директорки саме відмічають):

| Запит | ДО | Холодний (`nocache=1`) | Кеш-хіт |
|---|---|---|---|
| `getSalaryOverview&year=2026` | 45,5 с | 20,4 с (робота бекенду 11,6 с) | **5,3–5,6 с** |
| `getFillStatus` 1–15.09 | 62,5 с | **18,0 с** (11,4 с) — індекс дат | 8,4 с |
| `getFillStatus&date=2026-09-15` | 60,9 с | **11,4–13,0 с** (7,2–7,5 с) | 5,6 с |

Результати збігаються з v7.292 (summary заповнення 1–15.09 ідентичний; yearFact 15 з 17
локацій той самий, 2 змінились за ніч). Вранці в робочі години `ver_fill` bump-ається
кожним `saveAttendance`, тож два поспіль виклики дня дали промах (11–13 с), третій —
хіт: інвалідація працює як задумано, а холодний шлях уже не 61 с.

## getClients для списку: без полів картки і без повторення заголовків (v7.292)

Привід: `getClients` віддавав 2,37 МБ (1 246 карток × 37 колонок) на кожне відкриття
clients.html, index.html (дашборд) і meals.html. Що читає список у clients.html
(`renderClients`, `renderDebts`, `renderArchive`, `renderIncomplete`, `renderAbsRequests`):
ID, ПІБ, локація, група, вихователь, статус, дата народження, телефони, дата/сума/номер
договору, дати адаптації/розірвання і причина, відсутності (JSON — бейджі й запити),
графік внеску. **Лише картка** читає: JSON здоровʼя й розвитку, нотатки, свідоцтво,
місце реєстрації, документи й РНОКПП батьків, № додаткового договору.

Розклад ваги: «Відсутності (JSON)» 19 % (лишаються — їх читає список), здоровʼя +
розвиток лише 8 %, з нотатками й документами — 29 %; решта — назви 37 колонок,
повторені 1 246 разів.

Бекенд:
- `getClients&mode=list` — без 10 колонок картки (`CLIENT_HEAVY_COLS`);
  `&format=compact` → `{headers, rows}` замість масиву обʼєктів. Разом 2 410 → ~830 КБ
  (оцінка на вчорашніх даних, −65 %).
- `getClientCard&id=` — повна картка одним рядком (TextFinder по колонці A).
- `saveClient`: `_clientPreserveAbsent` — для важких колонок, чиїх ключів у payload
  немає, лишає наявні клітинки (обидва шляхи оновлення). Без цього картка, збережена
  зі списку без довантаження, затерла б здоровʼя '{}' і документи ''.

Фронт:
- `clients.html`: запит `mode=list&format=compact`, `_expandClientsResp` → обʼєкти;
  `mergeCRMFromCloud` для відсутніх колонок лишає значення з localStorage і ставить
  `_lite`, якщо їх нема; `openChild` — при `_lite` довантажує `getClientCard` ПЕРЕД
  показом (тост «Довантажую картку…»), інакше оновлює у фоні; `buildClientPayload`
  при `_lite` не шле важкі ключі взагалі.
- `index.html`, `meals.html` — той самий запит; читають лише ПІБ/локацію/групу/статус/
  дату народження.
- Mock-тест у Node: list/compact/картка/NOT_FOUND; збереження без ключів лишає
  клітинки, з ключами — пише нові. cache v7.292.

Заміри на проді 16.09 (curl, 1 247 карток):

| Запит | Час | Розмір |
|---|---|---|
| `getClients` (як було) | 7,9–9,5 с | **2 374 627 Б** |
| `getClients&mode=list` | 8,2–8,5 с | 1 690 559 Б (−29 %) |
| `getClients&mode=list&format=compact` (так тепер просять сторінки) | **6,7–7,2 с** | **849 540 Б (−64 %)**, 28 колонок з 38 |
| `getClientCard&id=` | 5,1–5,9 с | 2,9 КБ, 38 полів |

compact ↔ full звірено по всіх 1 247 рядках — розбіжностей немає. Час обмежений
ціною виклику (~5 с); виграш у часі ~2–3 с, у трафіку — 1,5 МБ на кожне відкриття
Клієнтів, дашборду й харчування.

## Замок місяця на даних і експортах (v7.291)

Привід (аудит 15.09): «Закриті_Місяці» перевіряли лише 6 Payment-шляхів. Відмітки
додаткових, уроки предметників, Табель, обʼєднання/поділи, харчування, «Чомусики»
та експорти в Salary замок не читали; MONTH_LOCK (v7.272) стримував лише не-CFO і
лише в межах поточного місяця. Тож CFO (або прямий POST) міг поставити відмітку
15.07 і `exportToSalaryExtras({month:7})` переписував серпневу колонку Salary, яку
вже виплатили.

Семантика: закритий місяць = заморожено **і його дані, і його гроші**.
- `_closedGuard(date | {year,month})` — жорстка відмова `{ok:false, closedMonth:true,
  code:'CLOSED_MONTH'}` без винятку для CFO (вимикач — лист «Закриті_Місяці»).
  `_closedMonthsSet` тепер memo на час виконання (bulk не читає лист на кожен рядок;
  `closeMonth` скидає).
- `_marksDateLockError` спершу перевіряє жорсткий замок, потім старий MONTH_LOCK —
  тож `removeAttendanceMark`, `bulkAttendanceMarks`, `bulkRemoveAttendanceMarks`,
  `savePredmetnykyLesson`, `bulkPredmetnykyLessons`, `deletePredmetnykyLesson`
  отримали замок без змін у своєму коді (per-row skip, як і раніше).
- Додано напряму: `addAttendanceMark`, `saveAttendance` (закриті записи
  пропускаються, у відповіді `skippedClosed[]`, решта пишеться), `deleteAttendanceRecord`,
  `saveDopMerge`/`deleteDopMerge`/`saveDopSplit`, `savePredMerge`/`deletePredMerge`/
  `savePredSplit` (при видаленні за id — дата з рядка), `addMealMark` (per-mark skip),
  `addChomusykyMark`/`removeChomusykyMark`, `importPredmetnykyLessons` (`skippedClosed`).
- Експорти, лише при реальному записі (`dryRun` вільний): `_closedGuardExport(year,
  month)` — відмова, якщо закритий джерельний місяць **або** цільовий (N+1):
  `exportToSalaryExtras`, `exportPredmetnykyToSalary`, `exportPredmetnyToSalary`;
  `exportAttendanceToPayments` і `exportMealToPayments` — відмова при закритому
  `month`, а в повному прогоні пропускають ще й target-місяці, чиє джерело закрите
  (інакше перерахунок обнулив би їх); `salaryAddExtrasPayments` — пропуск позицій
  із закритим місяцем. Нічна гарантія й так пропускала закриті місяці — без змін.

Фронт (`activities.html`, `predmetnyky.html`, `clients.html`, `index.html`):
`_isClosedMonthISO` у `isWeekLocked` — дні закритих місяців сірі й для CFO; список
із `getClosedMonths` у localStorage на 5 хв. Помилки бекенду по окремих відмітках
уже показувались і відкочувались (v7.259/v7.272); додано те саме для зняття
відміток у місячній сітці додаткових. `saveAttendance` з `no-cors` відповіді не
читає — там захист лише через сіру клітинку + бекенд.

Перевірка на проді після деплою (POST через curl, липень закритий; усі без запису):
`addAttendanceMark` 15.07 → CLOSED_MONTH; `bulkAttendanceMarks` 10.07 → results[0]
MONTH_LOCK/CLOSED_MONTH, count 0; `saveDopSplit` 15.07 → CLOSED_MONTH;
`saveAttendance` з двома липневими записами → saved 0, skippedClosed 2;
`savePredmetnykyLesson` 15.07 від CFO (actorId 1) → CLOSED_MONTH;
`exportToSalaryExtras` month=7 dryRun → проходить (NO_MARKS), без dryRun → CLOSED_MONTH;
`exportAttendanceToPayments` month=7 → CLOSED_MONTH; month=8 → без замка (NO_MARKS).

Наслідок: дописати минулий закритий місяць тепер можна лише відкривши його в
«Закриті_Місяці». Зараз закриті червень і липень; **серпень варто закрити** — ЗП
виплачено, а замок його не тримає. cache v7.291.

## getAttendance: індекс дат, компактний формат, кеш для «всі» (v7.289)

Профіль 15.09 (curl, прод v7.284): будь-який getAttendance 12–15 с, і час **не
залежав від діапазону** — порожній тиждень (лютий, 21 байт) 11.8–17.8 с, місяць
усіх локацій (2 МБ) 14.6 с. Розклад: ~5 с — фіксована ціна виклику (`ping` 4.7–6.0 с,
1.8 МБ скрипта); ~6–8 с — `openById` + `getValues()` колонки «Дата» на всі ~47 тис.
рядків (Date-клітинки) на кожен запит; вікно/JSON/кеш — <1 с. Кеш для `loc=''`
(так просить clients.html) не працював узагалі: `if (loc)` + 351 КБ > ліміт 100 КБ.

Бекенд:
- **Індекс колонки «Дата»** (`_attDateIndex`, CacheService `attidx_v1`, TTL 6 год):
  `{день: [[перший, останній рядок], …]}` — сегменти підряд-рядків одного дня,
  `lr` (проіндексований lastRow) і відбиток `(дата, id)` останнього рядка. Запит
  читає лише хвіст після `lr` (зазвичай сотні рядків) і дописує індекс; повна
  побудова — при холодному кеші або коли відбиток не збігся (дедуп/видалення
  зсунули аркуш; `dedupAttendance`/`dedupAttendanceApi` ще й явно скидають індекс).
  Вікно = сегменти днів діапазону, злиті при розриві ≤ 300 рядків, кожен блок —
  окреме читання (>20 блоків → один min..max, як у v7.269). Пізня відмітка за
  старий день більше не змушує читати все між нею і основним блоком дня.
- **`format=compact`** → `data = {дата: {id: статус}}`; **`kind=kids|staff`** —
  без/лише рядки `STAFF::`. Обидва споживачі читали тільки ці три поля; «Ім'я»,
  «Локація», «Група» і ключі об'єктів були 65% байтів (231 КБ → 60 КБ на тижні
  всіх локацій). Без `format` — стара форма відповіді, без змін.
- **Кеш для будь-якого loc**, значення gzip+base64 (як `_leadClientIndex`): тиждень
  усіх локацій ≈ 21 КБ — влазить. Версія для «всі» — `attver_ALL` (bump у
  `saveAttendance` поруч із `attver_<loc>`).
- У відповіді `t` (мс по фазах: open/index/window/build/total, tailRows/buildRows/
  windowRows/blocks) та `idx` = hit|tail|build — для профілювання.
- Симуляція в Node на мок-аркуші: build → tail → hit → rebuild після видалення
  рядків; legacy і compact дають однакові дані; пізня відмітка потрапляє у вікно.

Фронт:
- `clients.html`: однолокаційні ролі просять **свою локацію** (`cloudLoc`), мережеві —
  усі; `format=compact&kind=kids`; ключ кешу включає локацію. Старий масив (бекенд
  до деплою) теж читається.
- `index.html` (`loadTabelFromCloud`): `format=compact&kind=staff`; старий масив теж.
cache v7.289.

Заміри ПІСЛЯ деплою v7.289 (curl): `ping` 4.0–5.9 с; **кеш-хіт 4.3–5.0 с** (`t.total`
60–67 мс — уся решта це ціна виклику); тиждень Круглої compact **6.7 КБ** (було
30 КБ, а clients.html тягнув 351 КБ усіх локацій); тиждень усіх локацій compact 79 КБ
(було 351), місяць усіх 446 КБ (було 2 МБ). АЛЕ `idx=build` на кожному запиті:
`buildRows=70715` (Табель уже 70 тис. рядків, не 47) і сирий JSON індексу не вліз
у 100 КБ CacheService — 15 локацій пишуть упереміш, день розсипався на сотні
сегментів. Фаза index 4.6–8.2 с. Виправлено у v7.290.

## Індекс дат: gzip і злиття сегментів (v7.290)

- `_attIdxAbsorb`: сегменти одного дня зливаються вже при побудові, якщо розрив ≤ 300
  рядків (той самий поріг, що при читанні) — у симуляції 70 тис. рядків / 131 день /
  8% пізніх відміток → 540 сегментів, 9 КБ сирого JSON.
- Індекс у CacheService — gzip+base64 (≈5 КБ); `t.idxBytes` у відповіді, `t.idxTooBig`,
  якщо все одно не вліз (тоді build, як раніше).
- Заміри після деплою v7.290 (curl, Табель 70 730 рядків):

  | Запит | Wall-clock | `t.total` (робота бекенду) | `idx` |
  |---|---|---|---|
  | ping | 4.5–6.4 с | — | — |
  | перший запит після деплою (build) | 12.5 с | 6.1 с (index 5.2 с, `idxBytes` **2.7 КБ**) | build |
  | тиждень Круглої, промах кешу | 7.3–8.9 с | **1.1–1.4 с** (index 90–190 мс, window 0.4–0.7 с) | hit |
  | місяць Круглої, промах | 6.8 с | 1.9 с (window 1.3 с, 13 272 рядки) | hit |
  | тиждень усіх локацій, промах | 6.8 с | 1.1 с | hit |
  | місяць усіх локацій, стара форма, 2 МБ | 9.3 с | 1.8 с | hit |
  | кеш-хіт | 5.7–6.4 с (один сплеск 11.8) | **40–150 мс** | — |

  Разом: робота бекенду на промаху 6–10 с → 1.1–1.9 с (індекс замість читання
  70 тис. Date-клітинок), на хіті — десятки мс. Wall-clock тепер майже цілком —
  ціна виклику Apps Script (ping 4.5–6.4 с, зі сплесками до 12 с незалежно від
  дії) — це вже пункт D із профілю: розмір скрипта 1.8 МБ. Тиждень виховательки:
  351 КБ / 13–16 с → 6.7 КБ / ~6–8 с. `tail` у замірах не зустрівся (між викликами
  ніхто не дописував Табель) — перевірено симуляцією.

## Порожній/прострочений токен → на логін (v7.288, фронт-онлі)

Привід: «Авторизація_Лог» за 15.09 — 198 із 529 безтокенних викликів це
`getDashboardNotifications` з однієї відкритої вкладки, де сесія жива, а
`token` порожній: бейдж задач опитував бекенд щохвилини без токена.

- Усі 16 сторінок із патчем `__mkFetchTokenPatched`: `_tokExpired(t)` читає
  `exp` із payload токена (base64url, той самий формат, що `_verifyToken` на
  бекенді); `_checkTok()` — якщо сесія має `user.id`, а токена нема або він
  прострочений → `_kick()` (чистить `mkids_session`, alert, `index.html`).
  Виклик: при завантаженні сторінки і перед кожним fetch до script.google.com
  (запит не відправляється). Сесії без `user.id` не чіпаємо — екран логіна і
  `_showStaleSession` ведуть їх самі. Нечитабельний payload → пропускаємо, хай
  вирішує бекенд.
- `clients.html`: `getUsers` для списку логіна — через `_tokUrl`; **`doLoginMain`
  зберігав сесію без `json.token`** (index.html — з токеном) — це і було
  джерело безтокенних сесій у тих, хто логінився з екрана Клієнтів. Виправлено.
- Наслідок після деплою: користувачі зі старою безтокенною сесією при першому
  відкритті отримають «Сесія закінчилась, увійдіть знову» — один раз.
cache v7.288.

## «Викладачі»: «8 занять × 800» замість «8 сес. × 800» (v7.287, фронт-онлі)

`activities.html`, вкладка Аналітика → Викладачі, розбивка ЗП для моделі
«За заняття»: скорочення «сес.» замінено на «занять» — як у підписі «Σ Разом»
під сіткою і в прев'ю експорту. cache v7.287.

## Прев'ю «Експорт → Salary» рахує як бекенд (v7.286, фронт-онлі)

`renderExportPreview` для «За заняття» рахувало дні × ставка, без обʼєднань і
поділів (Кругла, Лего: 2 дні = 1 600 замість 8 занять = 6 400); «За захід» узагалі
йшло як відмітки без суми.

- `activities.html`: блок Salary-прев'ю переведено на `_anaUnitsForModel` —
  ті самі одиниці, що в Аналітиці й бекендному `_dopCountUnits`: «За дитину» =
  відмітки, «За захід» = дати, «За заняття» = сесії з обʼєднаннями й поділами.
  Підпис кількості: «N занять» / «N захід(ів)».
- `_loadAnaMerges(force, range)` / `_loadAnaSplits(force, range)` — опційний
  діапазон [from, to] (inclusive); прев'ю вантажить обʼєднання/поділи саме за
  місяць експорту (EXP.month/year), а не за період вкладки Аналітика. Ключ кешу
  включає діапазон, тож перемикання між вкладками перевантажує коректно.
- Перевірено на живих даних Круглої за вересень: Лего 8 занять = 6 400, Футбол
  5 = 3 000, Чирлідинг 4 = 1 600, разом 11 000.
cache v7.286.

## Аналітика додаткових враховує поділи (v7.285, фронт-онлі)

Привід: Кругла, вересень — вкладка «Аналітика» показувала Лего 7 сесій × 800 =
5 600 ₴ (разом 10 200), а бекенд/Salary — 8 × 800 = 6 400 (разом 11 000).
Аналітика бачила обʼєднання (`getDopMerges`), але не поділи (v7.265): поділ
Baby-ki ×2 15.09 губився. Предметники (`_predSessionStats`, `countSessionsInRow`)
поділи вже враховували — там усе правильно.

- `activities.html`: `_loadAnaSplits` (getDopSplits за loc, зріз за періодом
  аналітики), `_anaSplitsMap` → `_anaCountSessions(gbd, merges, splits)` —
  група поза обʼєднанням множиться на parts; обʼєднання виграє, як у бекендному
  `_dopCountUnits` і в `_gridPayoutSummary`. Зачіпає KPI «ЗП», вкладки
  «Заняття» (Витрати/Прибуток) і «Викладачі» (розбивка), XLSX-експорт аналітики.
- Перевірено на живих даних Круглої за вересень: Лего 8 × 800 = 6 400, Футбол
  5 × 600 = 3 000, Чирлідинг 4 × 400 = 1 600, разом 11 000 — збіг із бекендом.
- Окремо помічено (не чіпала): прев'ю «Експорт → Salary» у тій самій сторінці
  (`renderExportPreview`) для «За заняття» рахує ДНІ × ставка без обʼєднань і
  поділів (Лего показало б 2 × 800) — стара оцінка, не те, що пише експорт.
cache v7.285.

## exportToSalaryExtras поважає dryRun (v7.284)

`exportAttendance({dryRun:true})` мав бути чистим читанням, але dryRun поважала
лише Payment-частина (`exportAttendanceToPayments`, v7.93); `exportToSalaryExtras`
ігнорувала параметр і писала в Salary та журнал (kind=salary) насправді.

- `exportToSalaryExtras`: `dryRun === true` → усе рахується як завжди (каталог,
  відмітки, обʼєднання/поділи, матч рядків, журнальна дельта), але `setValue`
  у Salary і `_commitJournalUpdates` пропускаються. У відповіді `dryRun`,
  `cellsWritten` (скільки клітинок змінилось би), `journalOps`. Guard NO_MARKS
  також повертає `dryRun`.
- `exportAttendance`: у відповіді `dryRun`; параметр і далі проходить в обидві
  частини.
- Поведінка без dryRun не змінилась.
cache v7.284.

## Куди йде сума в Salary — підпис на екранах (v7.283, фронт-онлі)

Привід: Кругла, Лего за вересень — система рахує 8 занять × 800 = 6 400 ₴
(3 з обʼєднанням 08.09 + 5 з поділом 15.09), як і директорка, але сума
стоїть у Salary в колонці **жовтня** (експорт додаткових і предметників пише
в наступний місяць), тож у вересневій колонці 0 і здається, що «система
показує інше».

- `activities.html`: під шапкою сітки — «Σ Разом за Вересень (усі групи):
  8 занять × 800 ₴ = 6 400 ₴ → у Salary за Жовтень · модель». Рахунок —
  дзеркало бекенду: усі групи локації для цього заняття за місяць сітки,
  обʼєднання схлопують, поділ множить, якщо група не всередині обʼєднання;
  моделі «За дитину» (відмітки) і «За захід» (дати) — свої одиниці. Без ставки
  в каталозі — попередження. Перевірено на Круглій: 8 × 800 = 6 400 → Жовтень.
- `predmetnyky.html`: до рядка «Разом за місяць» картки предмета додано
  «→ у Salary за <наступний місяць>».
cache v7.283.

⚠ Правку зроблено з клону репозиторію в scratchpad: macOS відкликав доступ
термінала до ~/Desktop (Operation not permitted). Локальна копія в
Desktop/BOT відстає — після повернення доступу зробити `git pull`.

## «Список дітей»: ознака — статус картки, не гроші (v7.282, фронт-онлі)

v7.281 (фільтр за бюджетом/фактом місяця) скасовано: дитина у відпустці або та,
що заплатила наперед, теж має бути в списку. Правило тепер (`_listIsLive`):
рядок Payment з карткою у статусі active / adaptation / future / paused → у
групі та в лічильниках; картка graduated / terminated або картки немає →
згорнута секція «⏸ Без активної картки» (ПІБ, група, статус картки або
«картки немає», бюджет місяця, остання оплата, 🎓/🚪/Картка). По мережі:
920 у групах, 20 згорнуто (14 закритих карток, 6 без картки). Пуща — 30 у
групах, тобто 10 вибулих із живими картками директорка закриває з екрана
«Потребують уваги» або через картку. cache v7.282.

## «Список дітей»: лише з грошима за місяць (v7.281, фронт-онлі)

Список брав усі рядки Payment-файлу, а рядки дітей, що пішли влітку, у файлі
лишаються з нулями — Пуща показувала 30 при 18 реальних, по мережі 940 рядків
при 849 з грошима за вересень (91 нульовий).

Тепер (`renderClients`, `calcStats`): в основних таблицях і лічильниках — лише
рядки з бюджетом або фактом > 0 за обраний місяць (`_hasMonthMoney`). Нульові —
у згорнутому `<details>` «⏸ Без бюджету за <місяць> (N)» наприкінці локації:
ПІБ, група, статус картки, остання оплата за 2026 (з Оплати-Рік), кнопки
🎓 Випускник / 🚪 Розірвати (ті самі модалки з датою; директорка — своя
локація) і «Картка». Блок «Нові клієнти» теж згорнуто. Працює для будь-якого
обраного місяця. Пуща: 20 у групах (17 ходять + Кутова у відпустці + 2 з
серпневими доп у вересні), 10 згорнуто, 4 нових згорнуто. cache v7.281.

## «Список дітей»: порядок і чистка (v7.280, фронт-онлі)

Було: блок «Нові клієнти» першим над усім списком і без фільтра статусу — 308
карток, з них 135 випускників і 74 розірваних упереміш із 98 живими; групи в
порядку блоків Payment-файлу; діти в порядку рядків файлу.

Стало (`clients.html`, `renderClients`/`buildGroups`):
- групи — за віком: miniBaby-ki → Baby-ki → Find-iki → Study-ki → Preschool →
  Чомусики → школа за класом (1 → 4); усередині типу — за назвою
  (`sortGroupsCanonical`, `GROUP_TYPE_RANK`); українські назви Благо мапляться
  через той самий `parseGroupField`;
- діти в групі — за алфавітом (uk), «за віком» лишилось перемикачем;
- «Нові клієнти» — лише статуси active/adaptation/future/paused, блок стоїть
  ПІСЛЯ груп Payment, підпис «картка є, у Payment ще немає», сортування
  локація → ПІБ; випускники/розірвані — в «Архіві», вибулі з Payment зі
  статусом active — у «Потребують уваги»;
- плитка «Дітей» рахує Payment + лише живі нові. cache v7.280.

## getLeads: індекс карток у кеші (v7.279)

`_leadClientIndex` (сума, дата договору, дата розірвання, статус по кожній
картці) тепер у CacheService на 5 хв. Сирий JSON — 145 КБ, понад ліміт 100 КБ
на ключ, тому зберігається gzip+base64 (≈24 КБ). Інвалідація при будь-якому
записі картки: saveClient, patchClientCell, deleteClient, mergeClientDuplicate.
Ефект — мінус одне читання аркуша «Клієнти» (2,3 МБ) на кожен getLeads.

## getLeads: formatDate у циклі (v7.278)

Той самий корінь, що й у getAttendanceMarks (v7.261). `_leadDayKey` для
Date-клітинок кликала `Utilities.formatDate` — сервісний виклик Apps Script —
і викликалась на кожен рядок історії лідів (×3), бота (×4) і на всі 1 245
карток у `_leadClientIndex` (×2): ~5 000 сервісних викликів на один getLeads.
Замір ДО: 19,9 / 20,7 / 34,5 с. Тепер Date → `_attDateFast` (локальні геттери,
пояс скрипта = Europe/Kiev). Рядкові дати обробляються як і раніше.
Замір ПІСЛЯ (той самий запит, 800 рядків, rowsTotal 4 162): 12,2 / 19,1 / 9,5 с.
Відповідь звірено поле в поле зі старою (day, excDate, cbDate, contractSum,
monthsStay, lifeDays по 800 лідах) — розбіжностей 0. Решта часу — три повні
читання аркушів (бот, історія 4 162 рядки, Клієнти 1 245) + агрегати за два
періоди; наступний крок, якщо треба, — кеш `_leadClientIndex` на 5 хв.

## Токен на решті дев'яти сторінок (v7.277, фронт-онлі)

analytics, categories, control, invoices, leads, opex, reconcile, salary,
salary_reconcile не мали `_tokUrl/_tokBody`, і всі їхні виклики Apps Script
ішли без токена. Замість правки кожного виклику — перший `<script>` на кожній
сторінці: хелпери `_authTok/_tokUrl/_tokBody` (як в index/clients) + обгортка
над `window.fetch`, що для URL Apps Script додає `?token=` у GET і `token` у
JSON-тіло POST. Покриває 46 наявних викликів (getPayments, syncPayments,
getGroupNorms, reconcilePreview/Apply, resolveIbanLoc, getOpexContractors,
getSalaryFopMap, getSalaryExtrasRows, getOpexData/Overview, addSalaryRow,
remindLead, getLeads, getInvoiceListData, generateInvoicePDF, getLocations,
getEmployees, salaryReconcileRows/Preview/Apply, getDashboardNotifications…)
і будь-які майбутні. Інші хости не чіпає; без сесії нічого не додає.
Після цього в «Авторизація_Лог» мають лишитись лише `getUsers` (логін) і
`authenticate`. cache v7.277.

## Номери договорів: відновлення з дат (v7.276)

187 клітинок «Номер договору» були датами: аркуш «Клієнти» колись працював у
US-локалі, і номер `05-03-35` Sheets прочитав як 3 травня 1935. Постраждали
рівно локації з кодом ≤ 12: Голосієво `08-…` (46), Кругла `05-…` (61), Бровари
`01-…` (50), Борщагівка `01-…` (25), Бігова `06-…` (5). Бекапи містять ті самі
дати (псування — в момент введення), але дата зберігає всі три частини:
місяць = код локації, день = друга частина, рік%100 = третя (30–99 → 19xx,
00–29 → 20xx; рік < 1000 — тризначний хвіст). Голосіївські номери збігаються з
місяцем/днем дати договору — підтвердження порядку.

`restoreContractNumbers {dryRun}` (POST): dryRun — план, дублі всередині
локації (11), збіги з наявними (1); apply — увесь стовпець T → Plain text,
запис номерів, HR_Audit `contract_number_restore` (до/після). `saveClient`
тепер ставить формат '@' на клітинку номера перед записом у всіх трьох гілках.

## «Неповні картки» для директорки (v7.275, фронт-онлі)

Екран у clients.html за зразком `empCompletion` у HR: активні (і на адаптації)
картки, де бракує обов'язкових полів — сума договору, дата договору, № договору,
дата народження, телефон мами. По кожній — червоні чипи з назвами полів
(«№ договору (дата замість номера)» — коли Sheets перетворив номер на дату;
«Телефон мами (є лише тата)»), 🔴 ≥3 полів / 🟡 1–2; сортування — спершу ті, де
бракує найбільше. Зведення: карток / повних / неповних / %, і прогрес-бари по
локаціях. Директорка — своя локація, CFO/мережеві — усі + фільтр локації.
Клік по рядку відкриває картку. Право `cards_completion` у матриці director/ceo
на всіх 15 сторінках; пункт «🧩 Неповні картки» у блоці «КЛІЄНТИ CRM».
Стан на 14.09: 1 015 карток, повних 267 (26 %); школи, Житомир і Благо — 0 %.
cache v7.275.

## Токени в поллінгу, кеш локацій, фантом «Осокорки сад» (v7.274)

**Токени (підготовка до AUTH_ENFORCE).** За тиждень 2 278 викликів без токена.
Виправлено джерела: поллінг бейджа задач `getDashboardNotifications` раз на 60 с
на 12 сторінках — тепер `&token=` із сесії (це були 1 080 записів); `saveAttendance`
без токена — табель співробітників (`index.html` pushTabelToCloud) і табель
дітей (`clients.html` flush) — обгорнуто `_tokBody`; `save`/`get`/`getAttendance`
в index.html — `_tokBody`/`_tokUrl`. 38 `patchClientCell` без токена — це
curl-правки CFO з терміналу 14.09, не фронт. Лишаються без токена сторінки без
хелперів `_tokUrl/_tokBody`: analytics, categories, control, invoices, leads,
opex, reconcile, salary, salary_reconcile (їхні власні GET/POST) — окремий крок.

**getLocations** — `CacheService` 5 хв (`locations_v1`): було ~8 с за 3 КБ на
кожен виклик з кожної сторінки і з 18 внутрішніх місць. `?nocache=1` або
`_invalidateLocationsCache()` після правки реєстру.

**Каталог додаткових.** Фантомна локація «Осокорки сад» — 10 позицій, чиї id
(48–58) збігаються зі справжніми «Осокорки» (ціни інші: Лего 340 проти 280).
Відмітки й Salary-експорт використовують рядки «Осокорки» (фільтр за локацією),
тож фантом лише засмічує каталог і робить `updateActivity(id)`/`deleteActivity(id)`
небезпечними (перший збіг id). Новий `setActivitiesActiveByLoc {loc, active:false,
dryRun}` вимикає їх за колонкою «Локація». Після деплою: dryRun → apply.

## «Потребують уваги» — відкрито директорці (v7.273, фронт-онлі)

Нове право `needs_attention` у матриці всіх 15 сторінок: `director` і `ceo`
(cfo/hr — через `all`). Пункт меню «✋ Потребують уваги» тепер у блоці «КЛІЄНТИ
CRM» на кожній сторінці (`goPerm('needs_attention')` → `clients.html#needs_attention`),
раніше висів лише внизу сайдбару clients.html під правом `clients_list`
(тобто був видимий і медсестрі, і виховательці). Nurse/vyhovatel його втратили.

Скоуп: `renderNeedsAttention` фільтрує `!isHR()` → лише `S.user.loc`; CFO і
мережеві ролі — усі локації (бекенд `getNeedsAttention` фільтра не має — це
наступний бекенд-крок, якщо треба серверна межа).

Екран: категорія `departed` (v7.262) малюється окремо — «зникла з Payment
<дата> · останній місяць у Payment»; у кожному рядку «на рішення»/«вибули» є
🎓 Випускник і 🚪 Розірвати — відкривають ті самі модалки `mGraduate`/`mTerminate`
з датою (і причиною / «куди пішов»), що й картка; доступ як у картці —
`canTerminate(loc)` (CFO/HR будь-де, director лише своя локація). Після
підтвердження рядок зникає (локальний статус ≠ active). cache v7.273.

## Замок місяця для відміток + серверна перевірка + історія дій (v7.272)

**Правило.** Директорка й вихователька редагують додаткові й предметників за
поточний місяць — від 1 числа до сьогодні включно (Київ). Наперед не можна.
З 1 числа наступного місяця попередній закривається — редагує лише CFO
(роль `cfo` або користувач id=1). Раніше було Пн–Пт поточного тижня, а на
вихідних — нічого.

**Фронт.** `isWeekLocked()` в `activities.html` і `predmetnyky.html` (назва
історична) тепер перевіряє `[1 число .. сьогодні]`; тости/тултипи «Місяць
закрито — редагує лише CFO». `saveBackend` у `activities.html` додає `actorId`
у кожен запис. cache v7.272.

**Бекенд — уперше перевіряє дату сам.** `_marksDateLockError(date, isCfo)` у
`bulkAttendanceMarks`, `bulkRemoveAttendanceMarks`, `removeAttendanceMark`,
`savePredmetnykyLesson`, `bulkPredmetnykyLessons` (add і remove),
`deletePredmetnykyLesson`. CFO визначається з токена (`_CURRENT_AUTH`), інакше
з `actorId`. Відмова — `code:'MONTH_LOCK'` по-рядково (пакет не падає цілком).
Об'єднання/поділ груп і відпустки не зачеплені. Самотест предметників іде від
actorId=1 — обхід збережено.

**Історія дій.** Що вже було в даних: «Додаткові_Відвідуваність» має Дата +
Час_відмітки + Відмітив; `Predmetnyky_Lessons` — Date + CreatedAt + CreatedBy;
видалення уроків предметників давно пишуться в HR_Audit (`pred_delete_lesson`).
Чого не було — видалень додаткових: тепер `bulkRemove`/`removeAttendanceMark`
пишуть у HR_Audit `dop_delete_mark` зі знімком рядка (дата, локація, дитина,
заняття, ціна, хто ставив). Звіт `?action=getMarksActivityReport&actorId=<cfo>
[&from&to&loc]` (read-only, лише CFO): по локаціях і людях — скільки відміток,
у які дні ставили, скільки заднім числом (lag ≥1 дня, ≥7 днів, max), наперед,
видалено; списки `backdated` і `deletions`.

## Автозвірка ЗП: сума не може лягти в чужий рядок (v7.271)

Привід: Оранж, Цуркан Марія — матчер знайшов директора, а сума пішла в рядок
медсестри (#29). Аудит 14.09: із 34 прив'язок «Звірка_Мапа» **33 вели в чужий
рядок**. Причина — мапа тримала `rowNum HR → rowNum Salary`; блоки локацій у HR
правлять руками, вставка рядка зсуває всіх нижче, і прив'язка переїжджає на
сусіда, а пріоритет мапи вищий за матч по ПІБ.

Що змінено (`salaryReconcilePreview` / `salaryReconcileApply`):
1. **Ключ** прив'язки — `emp.key`: `uid:<UID>` (HR кол. AA, v7.200), якщо UID
   виданий, інакше `nm:<норм. ПІБ>`. ІПН не став ключем (є в ~15% карток і
   з'являється саме під час звірки).
2. **Ціль** — нормалізована назва Salary-рядка (`salaryRowNorm`, поруч сира
   `salaryRowRaw`); рядок шукається за назвою, рівно один збіг — інакше
   прив'язка не діє.
3. **Запобіжник `_salaryRowGuard`** на всіх трьох шляхах (ручний вибір, мапа,
   ПІБ) і ще раз безпосередньо перед `setValue`: прізвище співробітника має
   стояти в назві рядка ЦІЛИМ словом (для слів від 5 літер — з похибкою и/і);
   прізвище іншого співробітника локації, включно зі звільненими → `foreign`,
   не пишемо → `need-salary-row` з поясненням. Безіменні рядки («Директор»,
   «Помічник») дозволені лише при явному виборі людиною або її ж прив'язці.
   Кілька Salary-рядків з одним ПІБ → не вгадуємо, просимо вказати.
   Без співробітника взагалі не пишемо.
4. **Нова мапа «Звірка_Мапа_v2»**; стара «Звірка_Мапа» більше не читається
   (лишена як архів) — усі 33 криві прив'язки знято, матч знову за ПІБ.
   Прив'язка зберігається лише для рядка, що пройшов запобіжник.

Аудит уже записаного: `?action=diagSalaryReconLog&months=8,9` (read-only) — кожен
рядок «Звірка_ЗП_Лог» за вказані місяці проганяється через той самий запобіжник
(«Співробітник» vs «Salary_рядок»); `mismatches` = сума лягла в чужий рядок
(власнику — зайве, співробітнику — не долетіло), `review` — безіменні рядки й
записи без співробітника, перевірити оком. Revert-нуті записи в лозі відсутні.

Перевірка після деплою: `?action=diagSalaryReconMap` (read-only) — прив'язки v2,
для кожної `guard: own|anon|foreign|…`, і `oldRowsIgnored` — скільки рядків
у старій мапі ігнорується. Фронт: `salary_reconcile.html` показує причину
(⛔ …) під співробітником, коли авто не спрацювало; cache v7.271.

Перевірено локально на живих даних: усі 110 авто-матчів за ПІБ по мережі
проходять; зі старих 34 прив'язок запобіжник блокує 29, ще 5 — це безіменні
рядки або рядки людей, яких у HR немає взагалі (пропускаються лише при явному
виборі людиною).

## Лічильник рядка групи рахує поділ (v7.270)

Фронт-онлі, бекенд не змінювався.

`countLessonsInRow` рахував ДАТИ з уроками й нічого не знав ні про поділ, ні про
зведення назв груп, тоді як «Разом» у шапці (`_predSessionStats`) рахує СЕСІЇ.
Через це в Манхетені «Дослідники 4-5» із поділом 07.09 показували `3/15` при
чотирьох оплачуваних заняттях, хоч підсумок зверху був правильний — `12
(+1 поділено)`.

Тепер обидва числа рахуються за однією логікою:

- `countSessionsInRow` — те, що показуємо в лічильнику: обʼєднання виграє над
  поділом (як у `_predSessionStats` і в бекенді `_dopCountSessions`), поділ дає
  `parts` замість 1;
- групи зводяться через `_predMrgNorm` — раніше урок із назвою, що
  відрізняється лише регістром чи пробілом, потрапляв у підсумок, але не в рядок;
- `countLessonDatesInRow` — окрема функція для **ліміту норми**. Ліміт лишається
  по датах, бо бекенд `savePredmetnykyLesson` рахує саме дати: якби ліміт поїхав
  на сесії, фронт блокував би відмітку раніше, ніж це зробив би сервер.
- коли числа розходяться (є поділ), у клітинці лічильника з'являється підказка
  «N занять у сітці · M оплачуваних сесій (поділ) · норма K на дати».

Перевірено симуляцією на реальному зрізі вересня: з уроком 07.09 у «Дослідники
4-5» рядок дає `4 / 15`, а `_predSessionStats` — `{raw:11, merged:11, final:12}`,
тобто рівно ті числа, що на екрані.

## Вікно рядків у getAttendance (v7.269)

Табель відкривався 45 с. Причина була не в фільтрі дат — він працював, — а в тому,
що фільтр застосовувався **після** зчитування всього аркуша. `Табель` — 47 427
рядків (10.8 МБ); на екран тижня потрібно 2 106 (4.4%), на місяць однієї локації —
475 (1%).

Що змінено в `getAttendance`:

1. **Вікно рядків.** Коли задано `from`/`to`, спершу читається ОДНА колонка «Дата»
   (у 6 разів менше даних), далі — лише блок від першого до останнього влучного
   рядка. Аркуш майже впорядкований за датою (93.8% сусідніх пар не спадають), тож
   блок вузький. Беруться min..max індексів, тому безлад у порядку рядків нічого
   не ламає: жоден влучний рядок за межі блоку не випадає.
2. **`_attRowIso` замість `_attDateIso` у порядкових циклах.** Для `Date`-клітинок
   дата рахується на місці (як у `_attDateFast`), без `Utilities.formatDate` — це
   виклик Apps Script API, і на 47 тис. рядків він коштував більше, ніж читання
   аркуша. Рядкові дати ('DD.MM.YYYY', довгі date-string) як і раніше йдуть через
   `_attDateIso` — поведінка для них не змінилась.

Той самий цикл є у `saveAttendance`: він проходив увесь аркуш **на кожну відмітку
пакета** (13 відміток × 47 тис. рядків = 600 тис. викликів `formatDate`). Тепер
теж через `_attRowIso`. Так само у двох дедуп-утилітах.

Фронт: `loadTabelFromCloud` (табель співробітників, index.html) знову передає
`from`/`to`. Коментар «getAttendance з from/to ЛАМАЄТЬСЯ» був хибний і застарілий —
через нього тягнули 1.5 МБ на локацію й фільтрували на клієнті.

## Поділ груп на підгрупи (v7.265)

Зворотна операція до обʼєднання: одна група одного дня = **два заняття викладачу**.
Дитині — одне (її сума йде з відміток, не з сесій). Норма не зачіпається — вона
рахує **дати**, а поділ дат не додає.

Де показується: додаткові з моделлю **«За заняття»** і **предметники**.
У «За дитину» і «За захід» кнопки немає (там платять за відмітки / за дату).

Аркуші створюються автоматично при першому збереженні в CONFIG-таблиці:
`Додаткові_Поділ`, `Predmetnyky_Поділ`. **Разових дій після деплою не потрібно.**

Правило перетину: **обʼєднання виграє**. Якщо група входить у набір обʼєднання
цього дня, її поділ ігнорується (набір = одна сесія за визначенням) — у модалці
така група показана сірою з міткою «у наборі».

Перевірка: `?action=dryRunSplits&loc=<локація>&year=2026&month=9` — read-only,
покаже всі записи поділу і скільки додаткових сесій вони дають.

**v7.266 — фікс шапки.** У v7.265 `insertSheet` + `setValues(шапка)` + `appendRow`
в одному виконанні клали дані **поверх шапки**: `setValues` буферизується, а
`appendRow` питає серверний `lastRow` (ще 0) і пише в рядок 1. Читачі починали
з рядка 2 і не бачили нічого — `dryRunSplits` віддавав 0 при збереженому записі.
Додано `SpreadsheetApp.flush()`, самолікування шапки при наступному записі
й читання по числовому `id` замість «пропустити перший рядок».

## Кілька виконавців одного предмета (v7.268)

`_normalizeSubject` зводить будь-яке «…психол…» в один `Психолог`, «…англ…» —
в одну `Англійська`. Поки на предмет один виконавець — зручно. Двоє — і позиції
каталогу стають нерозрізненними: у випадайці один пункт, `getRateFor` віддає
ставку ПЕРШОЇ позиції, а в ЗП **обидві** позиції читають один і той самий набір
сесій і кожна множить його на свою ставку — **один урок оплачується двічі**.

Ліки — точна назва в `CATALOG_TO_NORM_MAP`: вона перевіряється **раніше** за
фрагментні правила, тож кожна позиція дістає власний `subject_norm`.

| каталог | subject_norm |
|---|---|
| Психолог Ірина | Психолог Ірина |
| Психолог Маруфенко | Психолог Маруфенко |
| Кримська Юлія англійська | Англійська Кримська |
| Пахалюк Ксенія англійська | Англійська Пахалюк |
| Маришина Юлія англійська | Англійська Маришина |

**Ціна розділення:** уроки зберігають назву предмета, а не id позиції, тож
записані зі старою назвою осиротіють. Перед деплоєм звіряти
`?action=dryRunPredOrphanLessons` (read-only), перепризначати
`remapPredLessonSubject` (`dryRun:false` + `confirm:'YES_WRITE'`).
Кому саме віддати урок — вирішує людина: `EmpKey` в уроці це ХТО ПОЗНАЧИВ,
а не хто вів заняття.

## Картки під-локації — два допустимі місця (v7.267)

`_schoolRosterFromCards` для під-локації шукає картку спершу **на самій
під-локації** (група береться з картки як є), потім **під хостом** — але лише
якщо група картки збігається з групою блоку.

Навіщо: до v7.267 перенос карток на «Школа Кар'єрна» дав би `kept=0` — фолбеку
на цей випадок немає, і локація зникла б із «Оплати» разом із 203 550 ₴.

Доказ перед переносом: `?action=dryRunSchoolRoster&simulateMove=1` — рахує
ростер на індексі карток, ніби перенос уже зроблено. Нічого не пише.

## Чи відпрацювала нічна гарантія (v7.266)

`?action=getExportGuardStatus` — read-only. Віддає підсумок **останнього
завершеного** прогону (клітинок і сум окремо по Payment і Salary, скільки
локацій, коли) і `running:true`, якщо прогін іде зараз (`stuck:true`, якщо
висить понад 45 хв — норма ≈ 12 хв на 17 локацій).

Чому знадобилось: єдиним слідом був `Logger`, видимий тільки в Executions
редактора, а «Експорт_Журнал» пише лише **дату**, без години — ранкові записи
не відрізнити від денних.

## Застарілі копії — НЕ використовувати

- `~/Downloads/crm_script.js` — v6.11, травень 2026
- `crm_script.js.bak_16152` — v7.26, не відстежується git
- `~/Desktop/BOT/_backend_snapshots/`, `_RESCUE_19-08/` — архів аварії 19.08
- `/private/tmp/mk_recon`, `/private/tmp/mk_fresh2` — тимчасові, /tmp чиститься при перезавантаженні

## Разові дії після деплою v7.153

1. `IMPORT_LEADS_HISTORY_DRYRUN()` → перевірити лог → `IMPORT_LEADS_HISTORY_APPLY()`
   (створює аркуш `Ліди_Історія`; повторний запуск перебудовує його, не дублює).
2. `SETUP_MIRROR_SPREADSHEET_DRYRUN()` → `SETUP_MIRROR_SPREADSHEET()` — структура
   в окремій таблиці 1RLdzmff… + Script Property `LEADS_MIRROR_ID`.
   Потім `MIRROR_RESYNC_DRYRUN()` → `MIRROR_RESYNC_ALL()`.
3. `SEED_REPORT_RECIPIENTS()` → `installOwnerReportTrigger()` — тижневий звіт
   власнику щопонеділка о 9:00. Спершу написати боту в особисті, інакше
   Telegram не дасть йому почати діалог.
3. `installLeadsDigestTrigger()` — щоденне зведення по лідах о 9:00.
3. `clearAuthLog()` — з v7.151, ще не виконано.
4. `SEED_DIRECTORS_ME_DRYRUN()` → `SEED_DIRECTORS_ME()` — вписати
   @Ira_Melnichenk0 на всі локації для тестів (потім замінити на директорок).
5. `REPAIR_LEAD_CONFLICTS()` → `..._APPLY()` — почистити картки, де вже
   стоять одночасно екскурсія і передзвін (правило діє лише на нові дії).

## Ремап сиріт у Табелі (після деплою v7.163)

`REMAP_ORPHAN_ATTENDANCE_DRYRUN()` → звірити лог → `REMAP_ORPHAN_ATTENDANCE_APPLY()`.
Оригінали зачеплених рядків лягають у JSON-файл `BACKUP_Табель_ремап_<stamp>.json`
НА DRIVE (не в книгу — вона на межі 10 млн комірок). Саме він, а не lastRow,
є точкою відкату, бо ремап ПРАВИТЬ і ВИДАЛЯЄ рядки, а не тільки дописує.

`DELETE_MIGBKP_DRYRUN()` → `DELETE_MIGBKP_APPLY()` — прибрати липневі MIGBKP
(кожен аркуш спершу вигружається в JSON у теку `MIGBKP_архів_<stamp>` на Drive).

`CREATE_MISSING_CARDS_DRYRUN()` → `..._APPLY()` — 3 картки для дітей,
чиї відмітки нема куди перенести. Потім ремап підхопить їх за точним ПІБ.

`SETUP_LEADS_ANALYTICS()` → `installLeadsAnalyticsTriggers()` — вкладка
«Аналітика» в таблиці-дзеркалі: нічний перерахунок о 3:00 + меню
«m.kids → Перерахувати аналітику» в самій таблиці.

`AUDIT_SHEET_CELLS()` — скільки комірок з'їдає кожен аркуш і скільки звільнить
прибирання старих бекапів. Тільки читання.

`SETUP_AD_SPEND_SHEET()` — створює аркуш «Реклама_Витрати» у ТАБЛИЦІ-ДЗЕРКАЛІ
(тому спершу має бути SETUP_MIRROR_SPREADSHEET)
з випадайками каналу й локації. Заповнюється вручну.

## Прапорці

`TG_TEST` — константа в коді (рядок 627), **не** Script Property.
Поточне значення: `false` (робочі пороги нагадувань).

`AUTH_ENFORCE` — Script Property, **вимкнено** (`authEnforce:false`).
Фронтенд до вмикання підготовлено (v7.151), але сам прапорець свідомо не чіпали.
Перед вмиканням: очистити `Авторизація_Лог` через `clearAuthLog()` і подивитись 2–3 доби.

`WEB_LEAD_SECRET` — Script Property, **не задано**. Поки його немає, ендпойнт
форми з сайту відмовляє на будь-який виклик. Опис контракту — у `LEADS_WEB_FORM.md`.
